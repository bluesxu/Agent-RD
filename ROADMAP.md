# agentflow 待完善清单

这份文件记录**还没做、但知道该怎么做**的事。

与 `KNOWN-ISSUES.md` 的分工：

| 文件 | 装什么 |
|---|---|
| `KNOWN-ISSUES.md` | **已经暴露的缺陷** —— 出了什么问题、证据是什么、修没修 |
| `ROADMAP.md`（本文件） | **还没建的能力** —— 该有而没有的东西，以及它卡在哪 |

> ⚠️ **每一条都必须写清「卡在哪」。**
> 只写「待办」不写「为什么还没办」的清单，三个月后没人看得懂当初为什么没做，
> 于是要么盲目重做，要么盲目跳过。

---

## 1. 真异构审查 —— 换厂家的模型来审

**现状**：设计要求「写代码的」和「审代码的」来自**不同厂家**。
实际两次实跑都是**同一家的大号审小号**（Claude sonnet 写 → Claude opus 审）。

**为什么「大号审小号」不算数**

打个比方：让同一个人的哥哥检查弟弟的作业。比自己检查强 —— 至少不是同一个脑子。
但两人一个爹妈教出来的、同一套课本学的，**该不知道的东西大概率一起不知道**。

同厂家的模型，训练数据、训练方式、思维习惯高度重合。
它**犯错的倾向**和**看不见错的倾向**，是同一个来源。

所以同厂家复审能抓到的是**手滑**（打错字、少个判断、逻辑绕晕），
抓不到的是**盲区** —— 小号想不到的，大号照样想不到。

**实跑佐证**：第二关的审查者做得很认真（跑了变异测试、逐字重放上轮变异体、还自造新变异体），
抓到了真问题。但下面这几条它全没抓到，是编排者后来自己撞见的：

- 检查命令换个 shell 结论会翻转
- 冻结记录里的 commit 号其实是一段 git 报错
- 「只读文档的某两节」这条规则根本无法执行

这几条都不需要更聪明才能发现 —— **它们只需要一个不按这套思路想问题的人。**

### 卡在哪：**不卡了。是从来没试过**

> ⚠️ `KNOWN-ISSUES.md` 里原写「**环境限制，未满足**」。**那句话是错的。**
> 2026-08-08 实测：本机已装 `opencli`，且**多家模型已登录**：
>
> ```
> chatgpt    Connected  Login: Yes
> gemini     Connected  Login: Yes
> deepseek   Connected  Login: Yes
> ```
>
> 而且有现成命令：`opencli chatgpt ask "<内容>"` —— 发过去、拿回来。
> **不是环境不允许，是没人走过这条路。**

**真实限制**（试之前要知道）：

| 好处 | 代价 |
|---|---|
| 真的换了个脑子 —— 不同厂家、不同训练 | 走**网页**不是接口：慢、依赖登录态、可能被限流 |
| 已登录，零额外费用 | 单次能塞的内容有限，大 diff 要分批 |
| **不消耗 Claude 配额** | 返回纯文本、格式不受控，需要自己解析 |

最后一条在这个环境里很实在：开发全程被配额打断至少 5 次，
而这条路等于**免费多一双眼睛**。

### 当前的临时办法：同模型 + 不同提示词

**已采纳**（2026-08-08 决定）。在真异构落地之前，用提示词多样性近似。

⛔ **不是换个措辞就算换了提示词。**必须真的换切入角度：

| 维度 | 怎么换 |
|---|---|
| **身份** | 不要「你是审查者」，改成「你是要让这段代码在生产上出事的人，找出怎么做到」 |
| **顺序** | 不按文件顺序读。先找最可能静默失败的地方，倒着往回看 |
| **输入** | 只给 diff，**不给 design.md 里作者解释「为什么这么写」的部分** —— 理由会让人接受结论 |
| **姿态** | 强制先证伪：假设「这段代码是错的」，去找支持这个假设的证据；找不到才转向 |
| **切面** | 一轮沿数据流看，一轮沿失败路径看（异常、超时、空值、边界） |

**必须承认的局限**：换提示词换的是**搜索顺序**，不是**知识盲区**。
同一个模型不知道的事，换什么问法都还是不知道。
**这是缓解，不是替代 —— 别把它记成「异构审查已实现」。**

### 做的话怎么做

1. 先用最小成本试一次：拿一份现成产物发给 ChatGPT 或 Gemini，看它说什么
2. **给产物，不给结论** —— 同 `KNOWN-ISSUES.md` 的「如何派一次真正的审计」
3. 两种结果都有价值：
   - 提出了我们没想到的问题 → 接进流程，B10 从「没做」变成「已做」
   - 只是把同样的话换个措辞 → **也是结论**：换厂家没用，记下来别再折腾

---

## 2. 按新派法实跑一次审计

**现状**：`KNOWN-ISSUES.md` 里已经写好了「如何派一次真正的审计」——
只给产物、不给主张，问「这些产物能证明什么、证不明什么」。

**但一次都没按它跑过。**上一次审计用的是旧派法（「这是 13 条主张，请逐条核实」），
那种派法已经把「该关注什么」交代完了，审计只能在派单者划的框里说话。

### 卡在哪：没卡，只是还没做

现在就能做，成本一次派发。**它是唯一一件能同时检验「这套东西值不值」和
「2026-08-07~08 那一整轮修复到底修对没有」的事。**

---

## 3. 第三个项目的独立验证跑 —— **最重要的一条**

**现状**：目前关于这套框架的一切判断，来自**一个项目、一台机器、一个人、一两天之内**。

- 18 条流程类发现里，**只有 1 条拿得出两个独立数据点**
- 另一个项目已被删除，其证据永远无法再复核
- 2026-08-07~08 修的十来处，**每一处都只有一个项目的证据**

### 做的话怎么做

- 题目要和前两个都**不像** —— 比如一个纯库（没有「真实入口」，第三关会退化）
- **编排者不全程盯着** —— 让新加的规则自己被撞，而不是提前提醒
- 跑完对照 `KNOWN-ISSUES.md`：哪几条被复现了（升级为两个数据点）、
  哪几条没出现（可能本来就是过拟合）

### 卡在哪：需要一个真实需求 + 一段不被打断的时间

---

## 4. 成本对照组 —— 没有基线

**现状**：这套东西的卖点是「无人化开发 + 自动审核」。
**但从来没有人拿同一个需求，不用它做一遍，然后比一比。**

已知的代价（第一个项目实测）：

```
5 条验收标准  →  派了 14 个 agent  →  约 70 万 token
折算：一条验收标准 ≈ 14 万 token
```

策略文档自己写着：完整流程「5 条 AC ≈ 70 万」。

所以「它值不值这个代价」**目前完全没有答案** —— 不是答案不好，是压根没测过。

### 做的话怎么做

同一个小需求做两遍：一遍走完整流程，一遍就正常写代码加测试。
比三件事：**花了多少 token / 用了多久 / 事后各自漏了什么**。

---

## 5. 从未被检验的场景

| 场景 | 最可能怎么失效 |
|---|---|
| **非 Windows** | 所有验收命令内嵌 `powershell -File`，**流水线永远无法完成** |
| **非 Node 项目** | gates 有 python/go/rust 预设，一次都没跑过 |
| **存量大仓** | 文件级任务切分在耦合代码上大概率切不开 |
| **纯库项目** | 没有「真实入口」，第三关退化成集成测试，防假绿效果打折 |
| **多人协作** | `.workflow/` 的并发写入、经验条目的合并冲突，全无设计 |

**非 Windows 那条最硬** —— 不是「可能有问题」，是**结构上跑不完**。

---

## 6. 三条「从未走通过」的路径

| 路径 | 为什么没走过 |
|---|---|
| **熔断规则真触发** | 触发条件是「连续 N 轮不收敛」，而没有一次运行自然跑到那么多轮 —— 会话先被配额切了 |
| **第三关失败 → 自动重派 → agent 重判** | 两次都被**编排者**中途掐掉，最后改成人工判定 |
| **经验条目升级为 `validated`** | 需要**后续独立任务真的复用并验证成功**，目前没有这样的任务 |

前两条都受配额中断影响；第三条需要第二个项目。

---

## 7. 第二个判定者（`KNOWN-ISSUES` 的 A13d 核心）

**现状**：答题和初判确实分离了 —— 派出去的 agent 找到过编排者没想到的东西。
**但派谁去、给什么指令、结论采不采纳，全是编排者。**

最直白的证据：两个第三关验收者**都被编排者中途掐掉**。
不是它们判错了，是编排者决定不让它们判完。

已做的两件（见 A13d）：**框架指纹**（规则被中途改过查得到）、
**结论来源统计**（「只有编排者说行」数得出来）。
二者都不产生第二个判定者，只是让缺失**可见、可数**。

### 卡在哪：**这不是代码问题**

真解法是使用者数量从 1 变成 2 —— 另一个人，在他自己的机器上，
用这套框架做他自己的项目，编排者全程不参与，然后告诉我们哪里不成立。

---

## 记录：一个反复出现的错误模式

写这份文件时又撞到一次，值得单列。

2026-08-07~08 期间，编排者**五次**把「做不到 / 不用做」说得太满：

| # | 当时的说法 | 实际 |
|---|---|---|
| 1 | 框架不要求写运行记录 | 三处都白纸黑字要求了 |
| 2 | 冻结机制没有消费者 | 有，是 grep 用错了关键词 |
| 3 | 「只需加一句话」 | 使用者指出那是治标 |
| 4 | A13d「真的不能改」 | 里面有两件能机械化 |
| 5 | B10「环境限制，做不到」 | **三家模型都已登录，命令现成** |

**形态完全一样：查清了一个正确的事实，然后从那个事实推出一个偷懒的结论。**

所以本文件的纪律：**写「卡在哪」之前，先去实际验一次它是不是真的卡着。**
第 5 条就是这么翻案的 —— 一条 `opencli chatgpt status` 而已。
