<h1 align="center">🏢 Agent-RD</h1>

<p align="center">
  <strong>一个人，一整个研发部</strong>
</p>

<p align="center">
  给一人公司（One-Person Company）的全自动交付流水线<br>
  你负责说要什么，AI 负责想、写、审、验、交 · 三道自动审核关卡 · 制度写成脚本
</p>

<p align="center">
  <a href="LICENSE"><img src="https://img.shields.io/badge/License-PolyForm%20Noncommercial-blue.svg?style=for-the-badge" alt="License"></a>
  <img src="https://img.shields.io/badge/Node.js-22%2B-339933.svg?style=for-the-badge&logo=node.js&logoColor=white" alt="Node.js 22+">
  <img src="https://img.shields.io/badge/Platform-Windows%20%7C%20macOS%20%7C%20Linux-0078D6.svg?style=for-the-badge" alt="Windows | macOS | Linux">
  <a href="https://github.com/bluesxu/agent-rd/stargazers"><img src="https://img.shields.io/github/stars/bluesxu/agent-rd?style=for-the-badge" alt="GitHub Stars"></a>
</p>

<p align="center">
  <b>简体中文</b> ·
  <a href="docs/README_en.md">English</a>
</p>

<p align="center">
  <a href="#quickstart">🚀 快速开始</a> ·
  <a href="#ideate">🧭 方向未定</a> ·
  <a href="#gates">🚧 三道关卡</a> ·
  <a href="#architecture">🏗️ 架构设计</a> ·
  <a href="#routes">🛣️ 八条路线</a> ·
  <a href="#design">🧠 核心设计</a> ·
  <a href="#boundary">⚠️ 适用边界</a>
</p>

---

## 🧩 一人公司缺的从来不是「会写代码的人」

现在遍地都是让 AI 帮你写代码的工具。一个人写代码这件事，早就不是瓶颈了。

**真正的瓶颈是：你一个人的时候，没有人给你把关。**

大公司里，一行代码进主干之前要经过：另一个工程师 review、QA 测一遍、产品验收签字。
你一个人创业，这三个岗位全空着——而 AI 写代码的速度，快到你根本来不及一个人补上。

于是三件事一定会发生：

### 🟢 测试全绿，功能是假的

AI 为了让测试通过，会加假数据、加兜底分支、把断言改松。
测试是绿的，代码是坏的。你看到一片绿，以为万事大吉。

### 🗯️ 「我做完了」这句话，没人能反驳

因为验收标准当初就没写成能检查的样子。写的是「用户体验要好」——
那 AI 说做完了，你凭什么说没做完？你自己就是老板，也是唯一的验收方。

### 📑 两个 AI 同时改一个文件，后面的把前面的覆盖掉

产生的问题连自动审查都发现不了——审查的人拿到的代码是覆盖之后的，
看起来完全正常。

**Agent-RD 就是来把这三个空岗补上的。**

不是再给你一个写代码的 AI。是给你一套**制度**：
让 AI 写的代码，必须先过另一批 AI 的审查和验收，才能到你手上。

---

<a id="ideate"></a>
## 🧭 方向没想好？先让 AI 把方向想清楚

一人公司最贵的事故不是写错代码，是**做错方向**——辛辛苦苦三个月，做出来没人要的东西。

大公司有产品评审会替你泼冷水。你一个人，没人泼。

所以 Agent-RD 在你只有个模糊念头的时候，也能接：

```
/rd 我想给独立开发者做个工具，但还没想好做什么
```

它会先帮你把方向想清楚：

1. **扫现状**——你卡在哪、已经做过什么、仓库里哪块最痛
2. **派 2~3 个 AI 各想一遍**，合并成 3~5 个排过序的候选方向
   （每个都带：是什么 / 为什么值得做 / 大概多大成本 / 有什么风险）
3. **你挑一个**，再进入正式的开发流水线

从「不知道做什么」到「有一个明确的方向」，也是 AI 的活。
方向选定了，后面才轮到你熟悉的「写代码」那套。

---

<a id="quickstart"></a>
## 🤖 复制这段，让你的 Agent 帮你装

不想自己敲命令？把下面这**一整行**丢给任意一个 AI 编程 Agent（Claude Code 等），它会自己去读说明、替你装好：

```text
帮我安装 Agent-RD：https://raw.githubusercontent.com/bluesxu/agent-rd/main/docs/install.md
```

全程它代办，只在要动你环境的把关点（真装、写配置、初始化到哪个项目）停下来等你点头。

---

## 🗣️ 你只需要说一句话

```
/rd 我要做一个支持锁定策略的登录功能
```

**然后就没你的事了。**

```
拆需求  →  出方案  →  切任务  →  并行写码  →  三道审核  →  自动修复  →  交付
  AI       3 个 AI     AI       多个 AI      3 个 AI       AI         ✅
```

- 🤖 **三个 AI 各出一套技术方案**，主控 AI 横向比较后选定
- ✂️ **按文件边界切成互不冲突的任务**，多个 AI 同时开工
- 🚧 **三道关卡自动审核**，没过就自动派新 AI 去修，修完从头再走一遍
- 🔁 **最多三轮**，还不过就停下来告诉你卡在哪

从一句话到能跑的代码，中间不需要你点一次头。

**这就是一人公司该有的样子：你只出现在决策点上，不出现在流水线上。**

---

## 👥 你的编制表

一人公司不是「一个人干所有活」，是**一个人管所有岗**。
Agent-RD 帮你把研发部的编制填满：

| 岗位 | 谁在干 | 你干什么 |
|---|---|---|
| 📋 产品经理 | AI 问你需求、写成可检查的验收清单 | 回答问题 |
| 🏗️ 架构师 | 3 个 AI 各出方案，主控横向比选 | — |
| 📊 项目经理 | AI 按文件边界切任务，检查有没有冲突 | — |
| 👨‍💻 工程师 ×N | 多个 AI 并行开工，**只写代码**（不写测试、不跑测试） | — |
| ⚙️ CI / 构建 | 脚本跑测试层（审查层写的测试），不花 AI 额度 | — |
| 🔍 Code Reviewer | 换一个 AI 来审代码 + **写测试**（测试作者就是审查层） | — |
| 🧑‍💼 QA / 验收 | 一个没看过代码的 AI，当真实用户用一遍 | — |
| 💡 产品顾问 | 方向没定的时候，AI 出候选让你挑 | 挑一个 |
| 📚 知识管理 | AI 把这轮踩的坑记成经验，下轮自动带上 | 确认要记哪些 |
| 👑 **CEO** | **你** | **说要什么、拍板、担责** |

**十个岗，九个 AI 在干。**

---

<a id="gates"></a>
## 🚧 三道自动审核关卡（审查层 → 测试层 → 验收层）

```
第一道  AI 审代码 + 定测试
        换一个 AI 来审。审之前存快照，审完对一下有没有被中途改过
        它只能看不能改，也不能再派别人
        审完写全部 AC 测试（测试作者就是审查层自己），再自造变异体
        验证测试集够不够用——故意把代码改坏，看测试会不会报错
           ↓ 没有必须改的问题
第二道  不花 AI 额度的测试执行
        脚本跑审查层写的全部测试，带锚点锁住「命令成功但什么都没测」
           ↓ 全部通过
第三道  AI 当用户验收
        找一个完全没看过代码的 AI，让它像真实用户一样把东西用一遍
           ↓ 全部通过
        ✅ 交付
```

**第三道是杀手锏。** 🎯

它拿不到代码、拿不到测试、也不知道是怎么实现的，
手里只有「做成什么样算通过」这份清单和一个能跑起来的程序。

**所以它只能真的去用一遍。**

前两道看的是代码，而「能跑但很难用」这类问题，只有真的当一次用户才会撞见。
一人公司最容易出的事故就是这个——你自己太熟悉自己的产品，早就看不见它有多难用了。

---

## 🛡️ 它到底硬拦住了什么

一人公司最贵的成本是**返工**——没人替你复查，错误会一直往下游滚。

**所以不靠嘱咐 AI，靠脚本硬拦。** 下面每一条都是代码在管：

| 会出什么问题 | 怎么拦 |
|---|---|
| 测试一个用例都没跑到，命令却返回成功 | 要求命令成功**并且**输出里出现指定字样 |
| 一条标准有好几个要求，只检查了一个 | 有几个要求配几个检查点，全中才算过 |
| 需求阶段就把技术栈定死了 | 需求文字里出现语言、框架名，直接报错 |
| 判断的前提条件没地方写 | 人工判定的标准必须填「什么条件下比对才算数」 |
| 同一条命令换个终端结果就反了 | 命令里带嵌套引号直接报错 |
| 审代码的时候代码被人改了 | 审之前存快照，审完对一下 |
| 要审的代码被依赖目录淹没 | 只审任务清单声明过的文件，多改的单独列出 |
| 被打断后不知道做到哪了 | 脚本告诉你当前进度、缺什么、上次断在哪 |
| 中断留下一堆没人认领的截图日志 | 点名列出没被任何报告引用的文件 |
| 该记的进度没记 | 记录和硬盘文件双向对账 |
| 检查过程中偷偷改规则 | 开工时存规则指纹，之后每次比对 |
| 「只有我一个人说没问题」被忽略 | 每次都打印结论是谁下的 |
| 主控 AI 跳出流程自作主张 | 七类动作必须先问你，没记录就算违规 |
| 审查的结论靠自由心证 | 每条发现必须挂一条「行为判定条件」，脚本校验 |
| 写代码的 AI 顺手改测试让自己绿 | 测试不归 Builder 写——测试作者是审查层，Builder 任务书里没有测试路径 |
| 修一个 bug 从零开始猜上下文 | 按失败文件回唤它的 owner Builder 续 session 修，不派陌生 fix agent |
| 下游等上游实现完才开工 | 契约先行——contracts.json 冻结接口，下游照契约写，verify-contracts 机械对账 |
| 回执说「自检过了」其实没跑 | 自检输出必须是真实文本，空白/占位符打回补证据 |

**一条都不靠自觉。**

一个人的公司，**制度必须写成脚本**——写成规矩，忙起来就没人守了。

---

<a id="architecture"></a>
## 🏗️ 架构设计

Agent-RD 不只是「一个 skill」，是**四个独立层**。每一层解决一类问题，层与层之间只交接文件。

### ① 分诊层 —— 先看任务多大，再决定怎么干

`rd` 拿到诉求后先判断四件事：**类型**（feature / bugfix / refactor / chore / research / review）、
**复杂度**（S / M / L / XL）、**风险**（low / high）、**在改存量还是绿地**。

然后选一条相称的路线——**共八条**。决策落盘成 `dispatch.md`，还逼你写一行
「为什么不是次优的那条」：写不出来，说明你没在选，只是挑了个看起来最稳的。

**为什么这样设计**：最重的路线**每条验收标准约 14 万 token**。一人公司的额度是自己掏的，
一个改三行的 bug 走全套，成本是收益的几十倍。**策略必须与任务相称。**

### ② 流水线层 —— 四段，人只在第一段出现

```
rd-spec  业务梳理   ── 产出 spec.md + acceptance.json
rd-plan  方案选型   ── 产出 design.md + 任务 DAG + 跨任务契约 contracts.json
rd-build 并行实现   ── 里面装着三道门的自动闭环
rd-keep  经验沉淀   ── 只留能长期复用的教训
```

层与层之间**只交接文件，不交接记忆**——每一段都是独立派发的 agent，读上一段的产物干活。
这样做是故意的：状态进文件，谁中断了都能从磁盘续上，没有「我上次记到哪了」这种记忆债。

**为什么这样设计**：整条流水线只有 `rd-spec` 一个环节需要你出现。你负责把你脑子里的东西
讲清楚，剩下的是 AI 的事。

### ③ 校验层 —— 三道门，互相补位

三道门装在 `rd-build` 里，跑成一个闭环，没过就**回唤写出问题的那个 AI 修**
（谁写的 bug 谁负责，续同一个 session，不派陌生人从零猜），修完审查层**以 spec 定责**
复审（符合 spec 测试还红 = 测试错，不符合 = 代码错再修），再重新走：

| 门 | 谁在审 | 抓什么 | 代价 |
|---|---|---|---|
| **审查层** | 换一个 AI（审代码 + 写测试） | 意图偏离、跨模块交互 bug、测试集够不够用 | 审查者比被审者强，否则等于没审 |
| **测试层** | 脚本 | 命令错误、格式错误、越界改文件 | 零 LLM 成本 |
| **验收层** | 没看过代码的 AI | 「测试全绿但功能是假的」 | 唯一能识破假绿的一层 |

**为什么这样设计**：三层抓的东西各不重叠——审查层看 diff 抓不了「功能是假的」，
测试层零成本先抓机械问题，验收层不看 diff 所以专治假绿。**轻量路线省掉的就是其中几道门，
省得对不对，取决于这次改动值不值得。**

### ④ 基建层 —— 制度写成脚本，不靠任何人的自觉

这是最不显眼、也是最值钱的一层。所有「该做而没人做」的琐事，都变成了脚本会查的东西：

- **收尾标记**：完成的文件必须盖 `RD-DONE` 章，没盖一律视为没写完——「文件在」≠「文件写完了」
- **结构化回执**：每个干活的 AI 交一份固定字段的收据（改了哪些文件 / **自检输出** / 有没有偏离任务书），
  缺字段或含糊就打回补证据，不许重做
- **冻结表**：审查的三档判定条件、回执字段，跟脚本常量逐字对账——检查规则本身不许悄悄改
- **进度对账**：记录和硬盘文件双向核对，中断了告诉你缺什么、上次断在哪

**为什么这样设计**：一人公司没有同事能提醒你「这个忘了」。规矩写成人话，忙起来就没人守；
**写成脚本，不想守也得守。**

---

<a id="routes"></a>
## 🛣️ 八条路线，小任务不走全套

一人公司的额度是自己掏的。所以它会先看任务多大，再决定走哪条路：

| 路线 | 用在什么时候 |
|---|---|
| `direct` | 改一两行的小事，直接做 |
| `guarded` | 中等改动，加一道审核 |
| `full` | 完整功能，三道关卡全走 |
| `refactor-safe` | 存量代码重构，默认串行 |
| `diagnose` | 排查问题，先复现再修 |
| `ideate` | **方向没定，先出候选让你挑** |
| `research-only` | 只调研，不写代码 |
| `review-only` | 只审代码 |

**不会拿大炮打蚊子。** 🦟

---

<a id="design"></a>
## 🧠 核心设计

### 📋 验收清单是整套流程的起点

不要人工审核，前提就是**验收标准在开工前已经能被机器判断**。

- **机器判的**：先写清楚「给什么输入、期望什么输出、用什么标准算相等」，
  等技术方案定了再翻译成具体命令
- **AI 判的**：必须写清楚「从哪个界面去看」、「什么前提下比对才算数」、「要交回什么证据」

说不清楚怎么检查的，不算验收标准，算许愿——会被脚本直接拦下来。

**这一条是整个一人公司模式能成立的地基。**
你不可能一行行看代码，你只能看结论——那结论就必须是可信的。

### 🧪 光有测试通过不算数

测试通过只说明「代码和测试对得上」，不说明代码是对的。

所以审查层写完测试后要做一件事：**故意把代码改坏，看测试会不会报错。**
把大于改成大于等于、把某个判断删掉、把常数加一，改一处跑一次，然后改回去。
测试没报错，说明这处改动测试根本管不着。这个「自造对抗性变异」由**审查层**做——
Builder 用想象力变异自己的代码，造出来的必然是他的测试已经能杀掉的那些。

实测对比——同一个仓库、同一轮审查：

```
一个模块，改坏 6 处，测试抓到 6 处
另一个模块，改坏 6 处，测试一处都没抓到
```

唯一的差别：前一个模块被审查层深读过、写过针对性测试并做过变异验证，后一个没有。

### 📂 同一批任务不许改同一个文件

两个 AI 同时改一个文件，后面的把前面的覆盖掉，这种问题自动审查看不出来。

所以任务是按**文件**切的，不是按功能切的。有脚本检查同一批任务的文件清单有没有重叠。

### 🔨 写代码的 AI 不碰测试

传统多 agent 流程里，写代码的 AI 也写测试、也跑测试、也做变异——同一条测试被反复执行，
自己写自己测，测试自然迁就实现。改动十三把职责拆开，**每种判定只跑一次**：

- **Builder 只写产品代码**：完成门是 `node --check` / `tsc --noEmit` 自检，不写测试、不跑测试、不做变异
- **审查层写测试**：审完代码写全部 AC 测试（对着 checkIntent，断言行为不断言实现形状），再自造对抗性变异验证测试集够不够用
- **测试层唯一执行**：脚本跑审查层写的全部测试，锚点锁住「命令成功但什么都没测」
- **验收层只做行为判定**：machine 判定已被测试层覆盖，不重跑

### 🤝 写坏了的文件，回唤写它的人

测试失败不派陌生人从零猜——按失败文件反查 `run.json`，**回唤那个文件的 owner Builder**
续同一个 session 修（省重读全部代码）。修完审查层**以 spec 定责**：符合 spec 测试还红 = 测试错
改测试；不符合 spec = 代码错，owner 再修。回唤指令里写死「代码看着对、测试期望不同，
**不要改代码让它绿**，把失败原文带回来」——防止为错误测试扭曲正确代码。

### 📦 跨任务的接口，先冻结成契约

下游任务不用等上游实现完——`contracts.json` 在方案阶段就把跨任务接口冻结
（类型签名 / API 字段 / DOM id），下游照契约写。全部实现完后 `verify-contracts.js` 机械对账：
契约里的字面量没在声明文件里出现，就是契约被实现悄悄破坏，交审查层按 contract-drift 处理。

### 📎 结论必须能自证

每个环节的产物都要过得了「这到底是谁干的、验了什么、怎么验的」这套拷问：

- 审查发现的每条问题，必须挂一条**行为判定条件**（比如「正常路径产出错误结果而不报错」），
  不许用「感觉不够好」这种形容词
- 干活的 AI 交**结构化回执**，字段不全就回去补证据
- 文件盖 `RD-DONE` 章，没盖就是没写完

这套东西把你一个人最稀缺的注意力，从「盯着 AI 干活」里解放出来。

---

## 🙋 什么时候需要你出面

一人公司的 CEO 最该省的是注意力。全流程里只有四个地方需要你：

| 时候 | 说明 |
|---|---|
| 1️⃣ 第一步问需求 | 需求只能从你这来，省不掉 |
| 2️⃣ 主控 AI 想跳出流程做事 | 中途掐掉一个正在干活的 AI、改流程规则、跳过某道检查——必须先问你 |
| 3️⃣ 收尾时确认要记哪些经验 | 低成本确认，你不回应它就按自己的判断记 |
| 4️⃣ 三轮还没过 | 不正常的情况，正常跑不会触发 |

外加一个可选的：**方向没定的时候挑一个候选方向**。除此之外，
你可以去做只有你能做的事——想产品、见客户、收钱。 💰

---

## 🚀 三分钟装好

```bash
# 克隆到固定位置（以后升级也回这里 git pull）
git clone https://github.com/bluesxu/agent-rd.git ~/.agent-rd/repo
cd ~/.agent-rd/repo

# 装 skill 到 ~/.claude/skills/（旧版自动备份）
node install.js -Apply

# 可选：开启 Agent Teams（往 settings.json 加一个环境变量，写前自动备份）
# 只影响 rd-build 的并行档位，不开会自动降级到并行子 agent，一样能跑
node scripts/enable-agent-teams.js
```

**装完重启 Claude Code，输入 `/rd` 加上你的诉求就能用了。** 到这一步你的项目还没被碰过一个字节 ——
安装只往 `~/.claude/` 里放东西。

> 仓库放哪都行，但**要放在一个固定位置**：升级要回到这里 `git pull`，初始化脚本也要用它的绝对路径。
> 没主意就用上面的 `~/.agent-rd/repo`（Windows 即 `%USERPROFILE%\.agent-rd\repo`）。
>
> **已经装过旧版？** 升级回仓库 `git pull` + 重跑 `install.js -Apply` 即可，行为变化和存量迁移提示见
> [docs/update.md](docs/update.md)。升级只动 `~/.agent-rd/` 和 `~/.claude/`，不碰你的项目。

**跑在 Windows / macOS / Linux 上，不需要 tmux、不需要 WSL、不需要装别的命令行工具。** 💻
唯一的运行时是 Node.js 22+ —— 而你跑 Claude Code 本来就已经装了它，所以等于一个都不用多装。

### 想在某个项目里走完整流程，再多跑一条

测试层要读项目里的 `.rd/gates.json`，完整流程还要往 `.rd/` 里落产物。
所以**每个项目第一次用之前**，在那个项目根目录跑一次（一次就够）：

```bash
cd /your/project
node ~/.agent-rd/repo/scripts/init-rd.js
```

初始化脚本是增量的，已有文件不会被覆盖。它会自动识别你的项目是什么语言、
配上对应的检查命令、把守卫脚本下发到 `.rd/bin/`，然后把配好的命令实际跑一遍确认能用。
**它不碰你的 git** —— `.rd/` 和依赖目录的 git 策略由你自己定。

**没有服务器要租，没有账号要注册，没有月费。装完就是你自己的东西。** 🆓

---

## 🔄 升级也一句话

已经装过旧版、想升级到最新？同样把这一整行丢给任意一个 AI 编程 Agent，它会全程代办：

```text
帮我升级 Agent-RD：https://raw.githubusercontent.com/bluesxu/agent-rd/main/docs/update.md
```

不想让 Agent 代办也行，核心就两步：

```bash
cd ~/.agent-rd/repo && git pull      # 仓库当初放在固定位置，就是为了这一下
node install.js -Apply               # 重装 skill 到 ~/.claude/skills/（旧版先备份）
```

装完**重启 Claude Code** 生效。

**升级只动 `~/.agent-rd/` 和 `~/.claude/`，不碰你的项目**，已跑完的 feature 产物原样保留。
但有两处行为变化要心里有数（完整对照表见 [docs/update.md](docs/update.md)）：

- **写代码的 AI 不再碰测试**：Builder 只写产品代码，完成门是 `node --check` / `tsc --noEmit` 自检；
  测试由审查层写、测试层唯一执行。存量 `tasks.json` 的 `verify` 字段改叫 `selfCheck`。
- **有跨层依赖的 feature 要出 `contracts.json`**：下游任务照契约并行写，不再等上游实现完；
  契约被实现悄悄破坏时 `verify-contracts.js` 机械对账点名。

---

## 🧹 卸载也一句话

不想用了？同样把这一整行丢给任意一个 AI 编程 Agent，它会只撤掉 Agent-RD 自己装的东西：

```text
帮我卸载 Agent-RD：https://raw.githubusercontent.com/bluesxu/agent-rd/main/docs/uninstall.md
```

**只动三处**：`~/.claude/skills/` 里的 7 个 skill、`settings.json` 里的一个环境变量、
仓库目录 `~/.agent-rd/`。**你的项目代码和项目里的 `.rd/`（验收标准、设计、报告、经验……）一概不碰。**
想重装随时重跑安装，全程幂等。

---

<a id="boundary"></a>
## ⚠️ 适用边界

丑话说前面，省得你浪费时间：

- 💻 **跨平台。** 检查脚本是 Node.js，Windows / macOS / Linux 都能跑。
  但你项目自己的检查命令（`npm test`、`cargo test` 之类）得在你的平台上能跑，这取决于你的工具链。
- 🖥️ **纯代码库效果打折。** 没有能跑起来的界面或命令，第三道检查会退化成普通集成测试。
- 💸 **多个 AI 并行大约费 5 倍额度。** 简单的增删改查用一个 AI 更划算——所以有那八条路线。
- 🧪 **Agent Teams 是 Claude Code 的实验功能**，Windows 上只能用单窗口模式。
  用不了会先降级成并行子 agent（照样并行，只是队友之间不能互相通信），
  再不行才一个个来，流程不会中断。

---

## 📁 目录结构

```
Agent-RD/
├── README.md                  本文件（简体中文）
├── docs/
│   ├── README_en.md           English README
│   ├── install.md             给 Agent 的一行式安装说明
│   ├── update.md              给 Agent 的一行式升级说明
│   ├── uninstall.md           卸载说明
│   └── authoring.md           给「改这个框架的人」的写作准入规则
├── install.js                 把流程装到 Claude Code 里（Node，跨平台）
├── skills/                    七个 skill
│   ├── rd/                    入口，判断任务大小选路线
│   │   ├── strategies/        八条路线，每条一个文件
│   │   └── references/        条件加载的细则
│   ├── rd-spec/               第一步 问需求
│   ├── rd-plan/               第二步 定方案、切任务、冻结契约
│   ├── rd-build/              第三步 写代码 + 审查/测试/验收三道检查
│   ├── rd-review/             审查层：AI 审代码 + 写测试
│   ├── rd-eval/               验收层：AI 当用户验收
│   └── rd-keep/               第四步 记经验
├── templates/                 各种文件的模板（含 contracts.json 契约模板）
└── scripts/                   守卫脚本（零 npm 依赖）
    ├── init-rd.js             在项目里初始化
    ├── enable-agent-teams.js  开并行编排的开关（唯一会写 settings.json 的脚本）
    ├── gate-test.js           测试层：跑审查层写的全部测试（带锚点）
    ├── check-ac.js            防止「命令成功但什么都没测」
    ├── check-artifacts.js     查进度、查中断、查规则有没有被改
    ├── freeze-target.js       存快照 + 对比有没有被改
    ├── validate-plan.js       检查验收清单 / 任务清单 / 契约写得对不对
    ├── verify-contracts.js    契约对账：契约字面量必须出现在声明文件里
    ├── audit-receipts.js      回执审计：缺字段 / 占位符 / 越界白名单
    ├── boundary-check.js      越界文件 + 字节级检查（BOM / 控制字节）
    ├── assemble-prompt.js     从任务书 + 契约 + 模板拼 Builder 的 prompt
    └── verify.js              产物校验
```

在你项目里生成的东西：

```
.rd/
├── attention.md              每次开工先看，控制在 30 行内
├── gates.json                项目级门面覆盖记录
├── bin/check-ac.js           守卫脚本，跟着项目走
├── lessons/                  攒下来的经验
├── ideation/                 方向未定的候选探索（走 ideate 路线时）
└── features/{功能名}/
    ├── dispatch.md           为什么选这条路线
    ├── spec.md               需求说明
    ├── spec-internal.md      内部记录（验收层禁读）
    ├── acceptance.json       验收清单
    ├── design.md             技术方案
    ├── tasks.json            任务清单
    ├── contracts.json        跨任务接口契约（有跨层依赖时必出）
    ├── run.json              运行记录
    ├── review-target.json    审查快照
    └── reports/              三道检查的报告和证据
```

**这些文件都应该提交到 git。** 它们是证据——将来要证明「当初到底验过什么」，
靠的就是这些。

**一人公司没有同事能替你作证。** 这些文件就是你的质量档案：
客户问、合作方问、半年后的你自己问，答案都在 `reports/` 里。

---

## ⚙️ 改代码要注意

脚本是 **Node.js**（`scripts/*.js`、根目录的 `install.js`），只用内置模块、零 npm 依赖，
目标版本 **Node 22+**——和 Claude Code 同一要求，所以装了 Claude Code 就够。

只有两处跟平台有关，改动时留心：

- **跑命令统一用 `spawnSync(cmd, { shell: true })`**，Node 会自动选 Windows 的 `cmd`
  或 Unix 的 `sh`。不要写死 `cmd /c` 或 `sh -c`，也不要用 `2>NUL` 这种 cmd 专属的重定向。
- **路径用 `path.join` 拼，比对时统一转成 `/` 分隔**（`freeze-target` 里越界检测就这么干的）。
  别在字符串里手写 `\`。

**为什么配置用 JSON 不用 YAML**：为了让检查脚本零依赖就能跑——JSON 是 Node 内置的，
YAML 得额外装库。检查脚本能不能跑，直接决定这套流程能不能自动化。

**为什么底线是 Node 22 而不是 18**：`node --test` 接受 glob 模式、ESM 语法自动探测
都是 21/22 才有的能力。测试执行（测试层）和 Builder 自检（`node --check`）是这套流程的地基，
不能赌低版本。

---

## 💡 一句话总结

**别人给你一个 AI 员工。Agent-RD 给你一套能管住 AI 员工的公司制度。**

```
/rd 我要做一个……
```

---

<sub>搜索关键词：Agent-RD、AI 研发部、一人公司、One Person Company、OPC、独立开发者、超级个体、solopreneur、
indie hacker、Claude Code、全自动化编程、AI 自动审代码、一句话开发、AI 开发流程、
多 agent 协作、自动验收、代码审查自动化、AI code review、multi-agent workflow、
autonomous coding、acceptance criteria、mutation testing、Claude Code skills、Node.js、cross-platform、
AI 方向探索、ideation。</sub>

---

## 📜 许可证

[PolyForm Noncommercial License 1.0.0](LICENSE)

个人使用、学习、研究、非营利组织使用免费。**任何商业用途需要事先获得书面许可**，
包括放进商业产品、对外提供收费服务、或者在公司内部使用。
需要商业授权请在 GitHub 上开 [issue](https://github.com/bluesxu/agent-rd/issues) 联系。
