# agentflow 框架级缺陷清单

这份文件记录 **agentflow 本身**（skills / scripts / 流程设计）在实跑中暴露的问题，
不含被它开发出来的那些项目的业务 bug。

## 分类标准

一次独立审计要求把「发现」按可迁移性分开，因为混作一谈会让这套东西看起来比实际更经得起检验：

| 类 | 定义 | 判据 |
|---|---|---|
| **A** | 关于流程或 agent 行为的洞见 | 换个语言、换个系统、换个人做，也会撞到 |
| **B** | 某类工具的通性问题 | 概念通用，但记录的实例与修法绑死在特定工具/版本上 |
| **C** | 作者自己代码的 bug | **不是方法论发现**，只是脚本写错了 |

**C 类不该被算进「框架的发现」。**它们列在这里是为了完整，不是为了充数。

## 剔除标准（2026-08-07 复盘时补充）

这份清单是**边开发边记**的，因此天然带着过拟合：
把「这两个项目在这台机器上撞到的事」误当成「框架的发现」。复盘时按三条判据剔除：

| 判据 | 说明 |
|---|---|
| **① 与本框架无关** | 任何人用这个工具版本都会撞到，换掉 agentflow 照样存在 → 不是框架发现 |
| **② 自己制造的** | 我先加了一条过严的规则，它误报了，然后我把「误报」记成一条发现 → 循环论证 |
| **③ 通用知识** | 浮点、编码这类任何程序员都该知道的事，与 agent 工作流无关 |
| **④ 中断混淆** | 见下。**这条是本清单最大的系统性污染源。** |

### ④ 中断混淆 —— 最大的系统性污染源

本项目的开发环境是 **Claude Pro 计划，5 小时配额窗口很小，运行被频繁打断**。
本次会话中至少有 5 次因配额中断（含 agent 直接死于 `403 Request not allowed`）。

**后果**：任何形如「**从未走过 / 零触发 / 未落盘 / 没有产出**」的发现，
都**无法区分**下面两种情况：

| 真的是框架缺陷 | 只是被切了 |
|---|---|
| 框架根本没要求做这件事 | 框架要求了，但跑到那一步之前会话就断了 |

**这两者的修法完全相反**：前者要改设计，后者一个字都不用改。

**已经因此产生过一条错误发现**：A15 原本断言「`run.json` 没人写、L2 正文可以不写」，
复盘核实发现 `wf-plan:101`、`wf-build:245`、`wf-review:91` **三处都明文要求了**。
产物缺失的真实原因是「要求没被执行」，不是「没有要求」。详见改写后的 A15。

**纪律**：写下任何「从未 / 零 / 没有」之前，**先去 skill 正文里搜一遍有没有这条要求**。
搜到了就说明这不是设计缺陷，是执行缺陷 —— 两者都值得记，但记法完全不同。

**剔除方式沿用本框架自己的 `retired` 语义：标记，不删除，不重编号。**
删条目会让清单显得比实际更干净 —— 那正是这份文件要防的事；
重编号会打断 `run.json`、lessons、README 里的所有交叉引用。
被剔除的条目保留标题与一行理由，正文压缩。

## 证据来源

- 项目一：`agentflow-testrun`（短链服务，5 条 AC）—— **2026-08-07 已由用户删除，产物不复存在**
- 项目二：`ema-screener`（币安均线选币 CLI）—— **2026-08-07 走完全链路，每一层都有落盘产物**
- 一次独立审计：`AUDIT.md`（审计方看不到设计过程与作者结论）

**重要限制（一）**：项目一的 L2/L3 **报告正文**从未落盘（只有 `.diff`，没有 `.md`），
且 `run.json` 里写着 `l3: {passed:5, failed:0, blocked:0}` —— **一个没有落盘依据的数字**。
凡是标注「来源：项目一的 L2/L3」的条目，**产物层面不可复核**。
项目已删除，这些条目此后永远无法再被复核，只能按「作者自述」看待。

> ⚠️ 但**不要**把这条读成「框架没要求落盘」。`wf-review:91` 明文要求写
> `reports/l2-round{N}.md`。正文缺失的真实原因在「没照做」和「被配额切了」之间，
> 事后无法区分。见判据④ 与 A15。

**重要限制（二）**：项目二的 `run.json` 是**事后补写**的（文件内 `_provenance` 字段自述了这一点）。
运行期不可复原的字段一律标 `not_recorded`，未回填猜测值。见 A15。

**重要限制（三）—— 运行环境**：开发全程在 **Claude Pro 计划**下进行，
5 小时配额窗口小，**运行被频繁打断**（本次会话至少 5 次，含 agent 死于 `403 Request not allowed`）。
这不是背景噪声，是**贯穿整份清单的系统性混淆变量** ——
凡「从未走过 / 零触发 / 没有产出」类的条目都受它影响。见判据④ 与 **A19**。

**当前唯一可完整复核的链路**：项目二
`spec.md → design.md + tasks.json → l1-round1/2.json → l2-round1/2.md + .diff + review-target.json
→ l3-round1/2.md + evidence/ → run.json → lessons/×3 + attention.md`

但即便这一条也有污点：**AC-7 由人判定，AC-1~5 的现行 check 形式由编排者自验**，
不存在「7 条全部由隔离方判定」的记录。见 A13。

---

# A 类：流程与 agent 行为

## A1 · 自报不是证据

**现象**：一个 Builder 正确诊断出「转义序列会被静默解码成裸控制字节」，正确修了代码路径，
然后**在解释这个修复的注释里原样重演了同一个 bug**，并在报告里写「文件里不再需要任何转义序列，没有重犯风险」。
那句话是假的。

**四道防线全漏**：L1 过（注释里语法合法）、测试过（不影响行为）、L2 大概率漏（diff 渲染后裸字节不可见）、
L3 必然漏（不影响任何 AC）。**只有编排者拿字节流核对才抓得到。**

**状态**：已写进 `wf-build` 的边界核对一节。
**残留**：编排者查哪些维度仍由编排者自己决定，没有任何东西审计「他没查什么」。

## A2 · 覆盖面缺口 ≠ 断言造假

**现象**：reviewer 原话 ——「测试没有造假、没有 mock、没有 skip、没有放宽断言……
问题在覆盖面的选择性：它精确覆盖了 5 条 AC，然后停在了作者自己写的那些分支门口。」

**这比造假更隐蔽**：所有常规的「测试造假」检查都会通过。

**状态**：`wf-review` 已要求区分这两者。**根因未解** —— 见 A11。

## A3 · 边界约束不消除压力，只改变去向

**现象**：fix agent 面对「要暴露内部实例但无权改那个文件」的冲突时没有越界，
而是把变通挤进了它**有权改**的文件里（在路由处理器上挂了一个只为测试存在的属性）。

**白名单守住了，设计气味转移了。**

**状态**：已记录。这是白名单机制的固有代价，不是可以修掉的 bug。

## A4 · 文件不重叠 ≠ 可并行

**现象**：在存量代码上做重构时，三个独立方案**一致得出**核心部分切不开的结论。
其中一份精确定位到单点：某个测试辅助文件同时是两处改动的落地点，两个任务集**必然在它上面重叠**。

原话：「这次重构没有绿色的中间状态可停。」

**绿地项目里这个问题不存在**（新文件互不影响），重构里它是头号风险。
`validate-plan` 只校验同层文件互斥，**从不检查「某个 task 单独落地后仓库是否还自洽」**。

**状态**：已写进 `refactor-safe` 策略（默认串行，并行需论证）。**机械校验仍缺失。**

## A5 · 下游 prompt 不能照抄计划

**现象**：一个 Builder 为防 Promise 永久 pending，在计划外多加了一个错误码。
下游负责错误码映射的 Builder 如果照抄 plan 阶段写死的 steps，就会漏掉这个分支。

**计划是派发前写的，上游偏离是派发后发生的。**

**状态**：已写进 `wf-build`（派下游前必须读上游实际产出的签名）。

## A6 · 并行下 agent 无法自证边界

**现象**：两个 Builder 各自跑 `git status` 自查，结论**都是错的** ——
它们看到的是全部并行 Builder 加编排者改动的叠加状态，却当成了自己的。
其中一个还把编排者改的文件误报成「来自并行进程」。

**状态**：已写进 `wf-build`（明令禁止 Builder 自查，边界核对归编排者）。

## A7 · 熔断规则漏了第三种失败模式

原规则只覆盖「agent 卡住」和「agent 返回坏结果」。
**agent 死于外部原因**（配额耗尽、API 报错、进程被杀）既不是卡住也不是坏结果 ——
按字面执行会把它算成一轮失败，三次就熔断，而实际上一行代码问题都没有。

**状态**：已写进 `wf-build`（不计轮次，恢复后 `SendMessage` 续同一 session）。

## A8 · 返回契约缺失 —— 买了压缩又当场退光

**现象**：框架规定了**派出去什么**（自包含 prompt、文件白名单），
**完全没规定回来什么**。每个 agent 把完整报告原样倒进编排者上下文，
一场下来光 agent 报告就吞掉几十万 token。

而框架自己写着：「子代理的价值是把污染关进用完即弃的上下文，**只让蒸馏后的结论回流**。」

**派发成本照付，收益归零。**

**这一条是使用者发现的，不是作者。**

**状态**：已写进 `wf-build`（完整报告落盘，返回 ≤15 行）。项目二的 T4 起生效。
**额外收益**：报告落盘后，下游 agent 可以直接读原文，不必经过编排者这个会失真的中转。

## A9 · 验收标准写于 Stage 0，会把技术选型锁死在 Stage 1 之前

**现象**：`acceptance.json` 的 `check` 字段在 Stage 0 就写死了 `node --test`，
`gates.json` 也是 node 预设。**Python 方案从一开始就过不了机器判定。**
三个论证 agent 里有两个开头第一句就指出「技术栈其实不用论证，已被钉死」。

审计认为这是**最有价值的一条 A 类发现，而它没进 README**。

**状态**：✅ **已修**（2026-08-07）。

> ⚠️ **修之前先核实了一件事，结论修正了这条的性质。**
> 我原以为「锁死」是硬约束。查完发现：**`wf-eval:130` 的「不改 acceptance.json」只约束 L3 验收者，
> `wf-plan` 全文没有任何一条禁止修改它** —— 技术定完之后回头改那条命令，一条规矩都不违反。
>
> 所以它不是锁死，是**锚定 + 没人被告知它该改**。
> 我据此一度打算只加一句话了事。**使用者指出这是治标**：
> 「业务梳理不应该出现技术选型，只讨论业务」——
> 阶段 0 里有技术是**类别错误**，该在源头拿掉，不是在下游打补丁。

**根因在 `wf-spec` 的明文要求**：原文写着
「`judge: "machine"` —— **必须**给出 `check`：一条能跑、退出码即结论的命令」。
**阶段 0 被明文要求写一条可执行命令，而命令必然带着语言和工具。**
这不是谁疏忽，是设计写死的。

**改法：阶段 0 要判据，阶段 1 才要命令。**

| 阶段 | 字段 | 要求 |
|---|---|---|
| **0（wf-spec）** | `checkIntent` | 说清输入 / 期望输出 / 判等方式。**不许出现任何语言、框架、测试运行器、包管理器的名字** |
| **1（wf-plan）** | `check` | 技术仲裁完之后，把 `checkIntent` 翻译成选定栈的可执行命令 |

落地五处：

1. `wf-spec` 改成要 `checkIntent`，并写明「业务梳理只讨论业务」+ 正反例
2. `templates/acceptance.json` 加 `checkIntent` 字段与两条示例
3. `validate-plan`：`-Stage spec` 要求 `checkIntent` 且**技术词命中即报错**（已测 `node` / `pytest` 均被拦，干净判据通过）；`-Stage plan` 要求 `check`，`checkIntent` 缺失只警告（向后兼容，ema-screener 仍 PASS）
4. `wf-plan` 加「第五步：把 checkIntent 具化成 check」，三条要求：不许动 checkIntent（它是规格）、几个并列判据就几个锚点、翻译不动说明判据本身有问题
5. `init-workflow`：目录里没有任何语言标记文件时，`gates.json` 标 `_provisional: true` —— 那是**猜的**（默认猜 node），阶段 1 必须回来解决（已测：空目录标记、有 `package.json` 不标记）

**拷问力度不降反升**：写一条 `node --test -t AC-1` 很容易蒙混，它只证明你会写命令；
说清「给定这组输入，输出必须等于这组值，误差 < 1e-9」逼你把**判据本身**想明白。

**遗留**：ema-screener 的 5 条 machine AC 只有 `check` 没有 `checkIntent`
（`-Stage plan` 下 5 条警告）。补它等于反向重建当时的判据，暂未做。

## A10 · 修了但没接进流程 = 没修

**现象**：`check-ac.ps1` 写出来了，但在 README、7 个 skill、templates、`validate-plan` 里**零引用**，
模板还在教「退出码即结论」。两个项目能用上它，只是因为作者手工改了它们的 `acceptance.json`。
**任何按模板起的新项目，拿到的还是会空过的写法。**

审计实测：把 AC-1 的测试改名后其 check 仍退出 0。

**状态**：审计后已接进 `templates/acceptance.json` 与 `validate-plan`（带过滤条件的 check 不走守卫直接报错）。

## A11 · 变异测试有效，但不在框架里

**现象**：项目二的 L2 对同一个仓库做变异测试，结果是
**EMA 模块 6/6 变异体被杀，pipeline 模块 6/6 变异体存活**。

差别只有一个：**EMA 那个 Builder 的任务书里写了变异测试要求，pipeline 那个没写。**

变异测试是**唯一直接检验「测试集够不够用」**的手段 —— 测试通过只说明实现和测试一致，
不说明实现是对的。而框架里一条都没有，两次出现都是 agent 自发或作者临时写进 prompt。

**状态**：✅ **已接进框架**（2026-08-07）：

| 环节 | 改动 |
|---|---|
| `tasks.json` | 加 `mutationTargets` 字段 |
| `validate-plan` | 每个 `judge=machine` 的 AC，必须有一个覆盖它的 task 声明非空 `mutationTargets`（已反向测试：ema-screener 5 条全被拦下） |
| `wf-build` | Builder 义务：按固定算子表机械造 ≥5 个变异体，逐条报告，**存活 > 0 不算完成** |
| `wf-review` | Reviewer 两个动作：① 重跑上轮变异体作对照组 ② **自造新变异体**（这一步才真正检验测试集） |

**为什么 Builder 和 Reviewer 要分工**：Builder 用自己的想象力变异自己的代码，
造出来的必然是他的测试已经能杀掉的那些。实跑证据：round 2 的 reviewer 重跑上轮 6 个变异体
全部被杀（看起来修得很好），但**自造的两个变异体存活了** —— Builder 的修复是贴着已知变异体写的。

### 上线当天就抓到了东西

接进框架后立刻拿 `ema-screener` 的 `src/pipeline.ts` 做验证 ——
那正是 round 1 里 6 个变异体全存活、round 2 补了 6 条测试的模块。
按新算子表机械造 6 个变异体：**存活 5 个**。

其中最有价值的一条：**成交额门槛 `>=` 改成 `>`，测试全绿。**
根因是**同一条规则实现了两遍** —— `rank.ts:39` 和 `pipeline.ts:131`，
而 AC-4 那条「恰好等于阈值时通过（>= 而不是 >）」的边界测试只测了 `rank.ts` 那一份。
**`pipeline.ts` 那份才是生产路径上真正跑全市场 530 个标的的。**

所以 AC-4 的机器判定是通过的，但它实际锁住的只有一半 —— 而且从门外完全看不出来（同 A14）。

**这条也顺带说明了变异测试为什么不能只做一轮**：round 2 已经针对 pipeline 补过测试了，
补的是「上一轮变异体指出的那几处」，补完仍有 5/6 存活。
**贴着已知缺口补测试，补出来的还是已知缺口。**

**遗留**：`ema-screener` 的这 5 个存活变异体**尚未修复**，已记进该项目的 `attention.md`。
⚠️ 另外这条规则目前仍只有**一个项目**的数据支撑，未在第二个独立项目上复现。

## A12 · 三层门的价值不可证伪（审计最严重指控）

**现象**：项目一的 L2/L3 报告一份都不存在，而框架自己在 `wf-review:91`、`wf-eval:108`、
`wf-build:262` 三处明文要求必须写。

审计原话：**「如果三层门什么都没抓到，产物会长得和现在一模一样。」**

**状态**：项目二 T4 起强制落盘。**项目一的证据永久缺失，补不回来。**

## A13 · 验证闭环 —— 同人、同机、同天、同模型

**现象**：框架的设计者、需求方、编排者、核对者、判定通过与否的人**是同一个**。
这正是这套框架自己要防的「自证」，只是被搬到了上一层。

> ⚠️ **2026-08-07 拆分。**原条目把三件不同性质的事捆成一条，然后整条标成
> 「结构性问题，代码解决不了」。**那是拿最难的那一小块给整条挡箭** ——
> 其中有能动手的部分被这个说法盖住了。同类错误今天犯过两次（A15 三个分论断错两个、A17 整条撤回）。

### A13a · 期望值污染任务书 —— 🟡 已加警告

编排者把自己跑出来的数字写进 `tasks.json` 的 steps，Builder 就不是算出来的，是**抄回来的**。
下游任何 agent 的「独立验证」验的都是「有没有抄对」，不是「这个数字对不对」。

实物（ema-screener，现在仍在文件里）：

```
"标的筛选必须三条同时成立：… （实测得 530 个；854 个 symbol 里混有 …）"
"⛔ 已收盘根数 < 800 的标的硬性剔除并计数。实测 110 个里有 5 个不足 800 根"
```

`530` 和 `110` 都是我跑出来的，**没有推导式，而且会漂**（币安随时上下架标的）。
写进任务书之后，**没有任何环节会重新测一次**。

**污染分三档，严重程度差很多**：

| 档 | 例子 | 严重度 |
|---|---|---|
| 可独立复算 | `EMA_N(t) = P_t − (N−1)/2`，故 EMA21=389 | 轻 —— 给了推导式，谁都能自己算 |
| **经验测得** | `实测得 530 个` | **重 —— 只能采信，且会漂** |

**状态**：`validate-plan` 加了警告 —— steps 里同时出现观察标记词（`实测`/`期望值`/`跑出来`…）
和具体数值时告警；带推导依据（`代数`/`推导`/`公式`/`浮点`…）的放行。
ema-screener 上实测：3 条命中，其中 2 条是硬的。

**为什么只能是警告**：分不清「我实测的期望输出」和「设计参数」——
`minBars=800` 也是数字，但那是设计决策，本来就该写进任务书。
判据用关键词而非数值本身，因为**那几个词恰恰是作者自己在标注「这是观察到的结果」**。

> **修正一处我自己的误判**：原条目还把「验收器的匹配字符串写进任务书」
> （如「测试名必须以 `screener AC-1` 开头」）列为污染。**那是错的** ——
> 锚点必须事先约定，否则 check 匹配不到、或者空过。这是机制的**必要输入**，不是污染。
> 「锚点命中不证明正确性」这个真实弱点由 **A11（变异测试）** 和 **A14（多锚点）** 负责，
> 二者今天都已落地。

### A13b · ~~上一轮发现泄漏进 skill 正文~~ —— ⛔ 撤回，不是框架问题

原条目称「上一轮 L3 的发现被写进了 `wf-eval` 的 skill 正文，下一个 evaluator 开工前就拿到了答案」。

**使用者澄清：那次改动是一条口误的提示词导致的，不是流程产生的。**
一次性外部事件，不构成框架缺陷证据。撤回。

### A13c · 审计只做过一次，且由作者派发 —— 🟡 可改善

先分清哪部分是真独立的：

| 维度 | 独立吗 |
|---|---|
| 审计方看不到我的推理过程与结论 | ✅ **真独立** |
| 审计方拿到的是产物而非我的自述 | ✅ 真独立 |
| 审什么（范围）/ 怎么审（prompt）/ 何时审 / 采纳哪几条 | ❌ **全是我的** |

那次审计确实产出了全清单最重的一条
（「如果三层门什么都没抓到，产物会长得和现在一模一样」），**但它的视野边界是我划的**。

**改法不是「多做几次」，是换派法**。当前派法约等于
「这是它的 13 条能力主张，请逐条核实」—— 已经把「该关注什么」交代完了，
审计只能在我给的框架内说话。

正确的派法见本文件末尾的「如何派一次真正的审计」。**差别在于：
前者让审计验证我的说法，后者让审计自己形成说法。**

**仍改不掉的**：派单的是我，采纳哪几条的也是我。

### A13d · 判定者与设计者是同一个 —— 🟡 核心不能修，但里面有两件能做的

> ⚠️ **本条原标 ❌「真的不能」，是把话说满了 —— 这是同一天内第四次。**
> 「没有第二个判定者」确实修不了，但我用这句正确的话，把里面**两件能机械化的事**一起盖住了。
> 前三次（A15 三个分论断错两个 / A17 整条撤回 / A9 先说只需加一句话）都是使用者点出来的；
> 这次是被要求「先看看」时自己看出来的 —— **说明这个盖法我会反复用，不是偶发。**

#### 今天实际发生的事，比「没人判卷」更具体

| 我先做了什么 | 然后做了什么 | 最后宣布 |
|---|---|---|
| 给框架加一条新规则 | 回头改项目让它符合这条规则 | 「通过了」 |

同一天发生**三次**（`preconditions` 回填 AC-6/7、`mutationTargets` 回填 tasks.json、
`verified_by` 补 `orch-check-r3.md`）。

**用考试打比方：考到一半发现有道题做不出来，就把题目改了，然后宣布自己及格。**

每一步都可能是对的（大部分改动确实在修真 bug），但
**没有任何东西能区分「修好了一个缺陷」和「把挡路的规则拿掉了」。**

#### ✅ 已做一：框架指纹 —— 开考时把考卷拍照存档

框架自己早就有这个招数。`freeze-target.ps1` 冻结审查目标，理由是它自己写的：

> 审查最常见的失效方式不是审得不好，而是「审的东西已经不是最终的东西」。

**这句话反过来一样成立：规则若在评判过程中能改，「通过」就不值钱。**
而框架唯一没用这招管住的地方，就是框架自己。

`check-artifacts` 现在：首次运行把 `skills/ + scripts/ + templates/` 的 SHA-256
记进 `run.json`（路径也进 hash，改文件名同样算漂移），之后每次比对。

- 未漂移 → ✓
- 漂移且无说明 → **exit 3**，不判为通过
- 漂移且 `frameworkDriftAcknowledged` 里写了原因 → 放行并把原因打出来

**不禁止改框架** —— 禁止是错的。只要求留一句为什么。
已在本项目上实测三态（capture / drift / acknowledged），并写下了真实的漂移说明。

#### ✅ 已做二：结论来源统计 —— 把「只有我说行」变成一个数字

原先「验证闭环是单人的」是文档里的一段话，读完就过去了。现在是每次检查都打出来的一行：

```
结论来源：agent 判定 5 条 / 编排者自证 1 条 / 人工判定 1 条
   ⚠ 编排者自证的不计入独立验证 —— 出题、答题、判卷是同一个人。
```

> 实现时踩到一个坑值得记：最初按「该层没作为门跑过就跳过」统计，
> 结果 round 3 那条**编排者自证**凭空消失了 —— 因为它的 `l1` 是 `not_run_as_gate`。
> **恰恰是最该被数出来的那一条，被算法漏掉了。**
> 改成：只要显式标了 `*_verified_by`，无论该层是否跑过门都计入。

#### ❌ 仍然修不了的

这两件**都不产生第二个判定者**。它们只是让「题目被改过」查得到、「只有我说行」数得出来。

每个我派出去的 agent：prompt 是我写的 → 判据是我定的 → 结果由我解读 → 采不采纳我说了算。
**这条链永远终止在我这里。**再加十层只是让链变长。

现成的例子：两个 L3 evaluator 都被**我**中途终止，AC-7 最终由人判定。
框架的「L3 失败 → 自动重派 → agent 重判」至今没走过，而阻断者每次都是我。

A13a / A13c 都是**输入侧**的修补：别让下游拿到答案、别让审计只能在我的框架内说话。
它们都假设「输入干净了，判定就可信」。

**A13d 说的是：没有第二个判定者。**这跟输入干不干净无关。

**为什么加更多 agent 没用**：每个我派出去的 agent，
prompt 是我写的 → 判据是我定的 → 结果由我解读 → 采不采纳我说了算。
**这条链永远终止在我这里。**再加十层只是让链变长。

现成的例子：两个 L3 evaluator 都被**我**中途终止，AC-7 最终由人判定。
框架的「L3 失败 → 自动重派 → agent 重判」至今没走过，而阻断者每次都是我。

**但「完全没有第二方」也说过头了。**2026-08-07 一天之内，使用者纠正了我四次，
每一次都是内部机制抓不到的：

| 使用者指出 | 我错在哪 |
|---|---|
| 「什么都没有是因为配额达到限制了」 | 把中断误诊成框架/隔离缺陷 |
| 「无痕无法添加指标」 | 探到「对话框能打开」就宣布可行，没探到产出目标产物那一步 |
| 「不要搞这种过拟合的东西」 | 在修过拟合的过程中新造了过拟合 |
| 「不应该由你来测试」 | 最初那条批评本身 |

**没有一条是脚本、agent 或门抓到的。**

所以准确的说法不是「没有第二方」，而是：**第二方存在，但不成体系** ——
它靠使用者恰好注意到，不靠设计。

**什么才算真修好**：另一个人，在他自己的机器上，用这套框架做他自己的项目，
我全程不参与，然后他告诉我哪里不成立。
**这不是代码改动，是使用者数量从 1 变成 2。**

## A14 · `then` 是多子句的，`check` 只有一个布尔值

**现象**：AC 的 `then` 常常写着好几个并列要求，而 `check` 坍缩成「退出码 0 且输出里出现某串」一个布尔值。

**实例**（ema-screener AC-1，由 L3 evaluator 在隔离状态下指出）：
`then` 里有一句独立要求「初值必须用 SMA(period) 作种子而不是用第一个收盘价」。
但 `check` 只验证「有 ≥1 个名字含 `screener AC-1` 的用例且退出码为 0」。
从 TAP 输出能看到的 5 个 AC-1 用例名里，**没有一个提到 SMA 种子** ——
evaluator 无从判断这一条到底有没有被锁住，只能判 pass 并把缺口记下来。

**事后核实**：种子**确实**被断言覆盖了（`ema.test.ts` 有 `'seed at index n-1=2'`、
`'seed at index n-1=4 (SMA5)'`，夹具注释也写明「SMA(n) 种子零瞬态」）。
所以这不是覆盖缺口，**是可见性缺口**。

**为什么仍然算问题**：隔离层看不见的东西，就等于不存在。
`then` 有 N 个子句而 `check` 只有 1 个布尔值时，
「哪几句被锁住了」这个信息**在设计上就无法从门外观察到**。

**可能的修法**（未实施）：要求 `then` 的每个子句在测试名里有对应的可见锚点，
或允许 `check` 携带多个 `-MustMatch`，一条子句一个。

## A15 · 「写下了规则」与「规则被执行」是两回事，而没有任何东西检查后者

> ⚠️ **本条已按判据④ 大幅改写。**原文断言「落盘不是强制的 —— `run.json` 没人写、
> L2 正文可以不写」，**这是错的**。复盘核实：

| 原断言 | 核实结果 |
|---|---|
| `run.json` 没有环节被要求写 | ❌ **错**。`wf-plan:101`「通过后写 `run.json`（从模板起手）」；`wf-build:245`「每轮结束追加一行到 `run.json` 的 `rounds`」；`wf-build:228` 外部中断也要「记进 `run.json`，标明是外部原因」 |
| L2 报告正文可以不落盘 | ❌ **错**。`wf-review:91`「同时把这份报告写入 `.workflow/features/{slug}/reports/l2-round{N}.md`」 |
| `wf` 派发决策零产物 | ✅ **成立**。SKILL.md 只规定了「复杂度 ≥M 建 feature 目录」，**判定过程本身（类型/复杂度/风险/选了哪条策略/为什么）没有任何产物要求** |

**所以真正的发现不是「框架没要求」，是「要求写在那里，没人照做，也没有任何东西发现这件事」。**

### 证据（三个独立数据点）

1. **项目二**：`run.json` 运行期间根本不存在，收尾时才由编排者事后补写。
   三处明文要求全部落空。
2. **项目一**（README 自己记录的）：软约束遵守度统计里，14 次 agent 派发中
   文件白名单 9/9、不创建子 agent 14/14、贴真实输出 14/14 —— **agent 的遵守度极高**，
   唯一一条被违反的是「同一消息派出」，违反者是**编排者本人**。
   README 原文：「唯一的违反者是持有规则原文的那个。」

3. **lesson 的 `status` 字段**：`wf-keep` 规定 `observed` 是初始值，
   `validated` 只能「在独立的后续任务中确实采用并验证成功时升级」。
   而项目一产出的两条 lesson，frontmatter 写的都是 `status: validated` ——
   该项目唯一的后续 feature（`test-seam`）**从来没有被建过**，
   不存在任何独立后续任务。**这两条是出生时就被直接写成终点的。**
   （实物仍在 `examples/lessons/`，连同说明保留作反面教材。）

   > 这条尤其能说明问题：清单里原先写着「`wf-keep` 的生命周期从未走过」——
   > **真实情况不是「没人走」，是有人直接写了终点，而没有任何东西检查。**
   > 一个「从未发生」的判断，背后可能藏着一个「发生了但是假的」。

三个数据点指向同一件事：**规则对 agent 有效，对编排者无效。**
因为 agent 会被三层门检查，而**编排者不被任何东西检查**（见 A18）。

**共同形态**：规则都写在 skill 正文里、都写得很清楚、都没被执行、
**都是直到复盘才被发现的** —— 没有任何一条是被流程本身抓到的。

### 中断这个混淆变量没法排除

项目二的 `run.json` 缺失，究竟是「我没照做」还是「wf-plan 那一步跑到一半被配额切了」，
**事后无法区分**。本次会话至少 5 次配额中断，其中 agent 直接死于 `403 Request not allowed`。

**但这个不确定性不改变结论**：无论哪种原因，
**框架都没有任何机制在流程末尾检查「该有的产物是不是都在」**。
中断是常态而不是意外，一个假设「每一步都会跑完」的流程设计本身就是脆弱的。

### 仍然成立的窄结论：派发决策不可考

CCG 式的 7 条策略分派逻辑是本次新加的，
但因为判定过程零产物，**没有任何证据表明它被真正走过一次**。
「这次为什么选了 full 而不是 guarded」事后完全不可考。

### 修法方向（未实施）

1. **收尾产物清单校验** —— 加一个脚本，按 stage 检查该有的文件是否存在，
   缺失即判流程未完成。这条比「加更多要求」有用得多：
   **要求已经够多了，缺的是有人检查。**
2. `wf` 至少写一行决策记录（类型/复杂度/策略/理由），成本几乎为零。
3. 中断恢复时，先跑一次产物清单校验，确认从哪里续 —— 而不是凭记忆接着往下走。

## A16 · 判定的隐含前提没有容身之处

**现象**：AC 的 `given`/`when`/`then` 描述了「做什么、看什么」，
但**判定成立所依赖的前提**常常没有地方写，于是靠派发方口头传递 —— 换个派发方就漏。

**实例**（项目二 AC-7）：原文只写了「在 TradingView 打开这 3 个标的的 4h 图」。
没写**必须是永续合约图**（`BINANCE:<SYM>.P`）。
而币安现货与 U 本位永续是两条独立 K 线序列，同一时刻 O/H/L **每一位都不同**：

```
2026-08-07 12:00→16:00 UTC+8
永续 fapi:  O=0.19930  H=0.20390  L=0.19850
现货 api:   O=0.19940  H=0.20400  L=0.19870
```

有基差，EMA 必然差一截，差值大概率超出 1‰ 门槛。

**危害形态是「合规的错误」**：evaluator 打开现货图核对 → 读数对不上 →
判定「EMA 初值口径不一致」。它确实打开了 4h 图、确实挂了 EMA、确实逐个比对了，
**报告在形式上无懈可击，结论却是错的**。隔离机制对此完全无能为力 ——
隔离防的是「看了答案」，防不住「前提缺失」。

编排者在派单 prompt 里手写了 `.P`，但那不在验收标准里。
**口头补的前提不是前提，是运气。**

**与 A14 的关系**：A14 是「`then` 有 N 个子句而 `check` 只有 1 个布尔值」——
写下来的东西验不全。A16 是「前提根本没写下来」—— 该写的东西没进文档。
两者共同指向同一个设计缺口：**AC 的结构只能表达「怎么看」，不能表达「在什么条件下看才算数」。**

**修法方向**（未实施）：给 scenario 加一个 `preconditions` 字段，
`validate-plan` 对 agent 判定的 AC 强制要求非空 —— 与 `observe` 字段同等待遇。

## A17 · ~~冻结机制在任何 skill 里都没有消费者~~ → **整条撤回（本条断言是错的）**

> ⛔ **这条是我自己写错的，而且错法和 A15 一模一样：下断言之前 grep 用错了关键词。**

**我当时的"证据"**：`grep -i hash skills/wf-review/SKILL.md → 0 处`，
据此断定「冻结设计在任何 skill 里都没有消费者」，并把原 C4 从「脚本 bug」提升成「设计级失效」。

**复核结果 —— 机制是完整接通的**：

| 环节 | 位置 | 原文 |
|---|---|---|
| 创建冻结 | `wf-build:171` | `freeze-target.ps1 -Feature {slug}` |
| **强制核对** | `wf-review:18-28` | 「审查开始前，目标必须已被冻结……**你要做的第一件事是核对**」，校验失败 → **本轮作废**，返回 `TargetMoved` |
| 产物 | `ema-screener/.workflow/features/screener/review-target.json` | 真实存在，`sha256: f50f0229…` |

**skill 里用的词是「冻结」「SHA」「-Verify」，从头到尾没打算用 "hash" 这个词。**
我 grep 了一个它压根不使用的关键词，然后把「搜不到」当成了「不存在」。

**连带撤回的还有两句**：
- 「它被发现纯属偶然」—— **不是偶然**。那个 L2 reviewer 是在执行 `wf-review` 明文规定的第一件事。
- 「凡是没有人真的去用的验证机制，都应当默认它是坏的」—— 立论前提不成立。

**真相恰恰相反**：C4 那个 bug（记录的 hash 与产物字节不符）**是被流程本身抓到的**。
这是本框架里少数几处**机制真的生效了**的实例，我却把它写成了机制失效的证据。

**原 C4 归位到 C 类**（它本来就分类正确）。见 C4。

## A18 · 编排者自己的核对脚本没有任何东西检查（原 C7，复盘时提升）

**为什么从 C 类提上来**：原条目自己就写了「这条不是框架 bug，但值得单列」——
既然值得单列，它就不属于「作者代码 bug」那一类。

**现象**：编排者为了核对 agent 的产出，会临时写脚本。这些脚本和 agent 写的代码一样容易出错，
实测**五次核对有四次是脚本自己先错**（路径大小写、`Join-Path` 拼绝对路径抛异常、
`2>&1` 把 stderr 混进文件名数组、`Select-String` 返回 MatchInfo 而非字符串）。

**为什么这是流程问题而不是手滑**：整套框架对 agent 的产出设了三层门，
**却对「验证 agent 的那个人的工具」零检查**。
判定链条的最后一环 —— 编排者的临时脚本 —— 是整条链上唯一没有守卫的地方。

一个脚本 bug 造成的假 FAIL，会让一份正确的实现被打回重做；
造成的假 PASS，则直接穿透全部三层门。

**未修。**修法方向：编排者的核对结论也应当有可复核产物（命令 + 原始输出落盘），
而不是只在对话里报一个结果。

## A19 · 框架能处理「agent 死了」，不能处理「自己死了」

**这是本清单里唯一一条由运行环境直接逼出来的结构性缺陷，也是最该先修的一条。**

### 缺口的精确位置（grep 验证）

```
grep -E "中断|恢复|续上|配额" skills/*/SKILL.md
→ 全部命中集中在 wf-build:224-234
```

`wf-build:224-234` 有一套完整的**子 agent 外部死亡**协议：
配额耗尽 / API 报错 / 进程被杀 → 不计轮次、不触发熔断、记进 `run.json`、
不盲目重发、用 `SendMessage` 续同一 session、连续 2 次停下交用户。

**这套协议的前提是：编排者还活着，在旁边观察并决策。**

而**没有任何一处**处理「编排者自己的会话被切断」。整个框架隐含假设
**一次运行 = 一个连续不断的会话**。

### 这个假设在真实环境里不成立

开发环境是 **Claude Pro 计划，5 小时配额窗口**。
本次会话被打断至少 5 次（配额耗尽、`403 Request not allowed`、上下文压缩）。
**中断是常态，不是意外。**

### 观察到的四个具体后果

| 后果 | 证据 |
|---|---|
| **该写的产物没写** | `run.json` 被 `wf-plan:101` / `wf-build:245` 明文要求，运行期间始终不存在，收尾才补。缺失原因在「没照做」与「没跑到」之间不可区分（见 A15） |
| **恢复时靠猜** | 每次续上都是重新读文件、凭记忆推断进度。**没有任何机制能回答「我现在在哪一步、该有的东西哪些还缺」** |
| **中断被误读成缺陷** | 编排者曾把「L3 没有报告」诊断为框架/隔离问题，被用户直接纠正：「什么都没有是因为配额达到限制了」。**这是判据④ 的原始出处** |
| **孤儿产物无人认领** | `reports/evidence/` 下有 `r2-ac6-main.log`、`r2-ac7-detail.log`、`r2-tv-01~03.png` 五个文件，来自两个被中途终止的 evaluator，**没有任何报告引用它们**。框架没规定被中断的 agent 的部分产物该怎么处置 |

第四条最能说明问题：**这些证据文件是真实的、有价值的，但它们现在是垃圾**
—— 因为没有任何东西知道它们属于哪一轮、验证了什么、能不能采信。

### 为什么这条比它看起来重要

框架的核心主张是「无人化」—— 人不介入审核。
但**「无人化」隐含要求流程能跨越会话边界自我恢复**，
否则每次中断都必须由人来重建上下文、判断进度、决定从哪续
—— **人被赶出了审核环节，却在恢复环节被请了回来，而且这个环节完全没有设计。**

### ⛔ 一条被否掉的修法（记下来，因为它错得很典型）

我最初的修法是：**在策略文件里标注「能否在一个会话窗口内跑完」**，
并建议 `full` 策略「按 AC 切分推进，每条 AC 走完才算一个可中断点」。

**这是错的，两个层面都错**：

1. **把环境参数烧进了框架设计。**「5 小时窗口」是我这台机器上这个订阅计划的参数。
   写进本该环境无关的策略文档，等于让框架去适配某一个人的配额 —— 换个环境全部作废。
   **这是判据① 的过拟合，只不过这次是我在「修复」时新造出来的。**
2. **要求人去主观预估「能不能跑完」。**中断可以发生在**任何**时刻 ——
   派 agent 的那一秒、agent 跑到一半、写报告写了三行。预估必然出错，
   而且「按 AC 切分」这种人为切法只是把断点从一处挪到另一处，**并不消除断点**。

**正确的原则不是「算准了再跑」，是「任何时刻被切断都能从产物恢复」。**
差别在于：前者要求判断力，后者只要求纪律。**只有后者能机械化。**

### 修法方向

| # | 修法 | 状态 |
|---|---|---|
| 1 | **stage → 应有产物清单**，可执行的校验脚本。恢复时先跑它，取代凭记忆推断 | ✅ **已实施** —— `scripts/check-artifacts.ps1`。同时解掉 A15 的执行缺陷：**要求已经够多了，缺的是有人检查** |
| 2 | **`run.json` 加 `inflight`**：派 agent 之前写「此刻在做什么、派了谁、他该产出哪个文件」，拿到结果立刻清空。**`inflight` 非 null 就意味着上次被中断在这里** | ✅ **已实施** —— 模板加 `inflight` + `_inflightShape`；`wf-build` 4.2 节规定写入时机；`check-artifacts` 消费它并单独返回退出码 2 |
| 3 | **被中断 agent 的部分产物必须有归属**：要么在报告里认领（写清来源与可信度），要么删除。不允许无主证据留在 `evidence/` | 🟡 **半实施** —— `check-artifacts` 能点名孤儿文件（首次运行即抓到 6 个），`wf-build` 4.3 节写了规则；但认领动作本身仍靠人 |
| 4 | 恢复时的固定动作**顺序不可换**：跑校验 → 读 `inflight` → 逐个核对 `reportPath` → **以产物为准，不以记忆为准** | ✅ **已实施** —— 写进 `wf-build` 4.2 节与全部 7 个策略文件 |

**仍然缺的**：没有任何机制**强制**在 stage 边界调用 `check-artifacts.ps1`。
它现在是「可用」，不是「必用」——
按 A15 的教训，**可用但不强制的检查，等于不存在**。

---

# B 类：工具通性问题（概念通用，实例绑死）

## B1 · 过滤条件匹配不到任何用例时，运行器返回 0

**实例**：`node --test --test-name-pattern "<匹配不到的>"` → **退出码 0**。
`jest -t`、`go test -run` 等有同类行为。

后果：「只跑这条 AC 对应用例」的 check，在**这条 AC 的测试根本没写**时照样绿。

**修法**：`check-ac.ps1` 守卫，要求「退出码 0」且「输出里确实出现指定串」两个条件同时成立。
**通用的是机制，绑死的是判断依据的写法。**

## B2 · ~~空项目上机械门全绿~~ → 并入 B1

**并入理由**：与 B1 是同一个机制（**空过 / vacuous pass**）的两个入口 ——
B1 是「过滤匹配不到用例」，B2 是「根本没有文件可查」，
守卫都是同一条：要求输出里出现一个语义锚点，而不只看退出码。分开列会把一个机制记成两条发现。

保留的实质内容已并入 B1。原条目里那段「`mustMatch` 最初写成 TAP 格式串 `"# pass "`，
而 Node 默认输出 `ℹ pass`」是**我自己写错了锚点**，属 C 类，不是发现。

## B3 · ~~全项目检查器既不点名文件也不用通配~~ → 剔除（判据②：自己制造的）

`npx tsc --noEmit` / `eslint .` / `mypy .` 不点名文件，是这类工具**本来就该有的行为**。
它之所以成为「问题」，只因为我先加了一条「gates 必须覆盖 tasks.json 声明的全部源文件」的规则，
那条规则误报了它，我又加了 `coversAllSrc: true` 去豁免。

**这是一条我自己制造、又自己打补丁的循环** —— 记成框架发现是虚增。
（`coversAllSrc` 作为实现细节保留，但它不是一条「发现」。）

## B4 · 跨 feature 的测试名空间是共享的

**实例**：同一仓库里 feature-A 的 AC-4 和 feature-B 的 AC-4 会被同一个 pattern 一起匹配到。
更糟：**新 feature 的测试压根没写时，pattern 仍会匹配到旧 feature 的同名测试并全绿。**

**修法**：过滤串带 feature 前缀，测试名以它开头。

**为什么保留**：这不是某个工具的 bug，是「用名字做过滤」这一设计在多 feature 仓库里的固有后果，
换任何测试框架都成立。

## B5 · ~~`node --test <目录>` 在 Node 24 下把目录当模块加载~~ → 剔除（判据①：与本框架无关）

`node --test tests/` 报 `Cannot find module`，是 Node 某个版本的行为，
任何人写这条命令都会撞到，与 agentflow 无关。

它曾被当作「L1 抓到了别人抓不到的东西」的招牌例子。**但那个价值主张属于「模式一」**
（gates 里的命令从没被冒烟执行过），不需要靠一条 Node 版本事实来支撑。
留着它会让清单看起来更有分量，实际没有。

## B6 · ~~Node 原生类型剥离只剥注解、不做转换~~ → 剔除（判据①：与本框架无关）

`enum` / `namespace` / 装饰器过得了 `tsc` 但过不了 `node --test` 直跑 `.ts`。
纯 Node/TypeScript 工具链知识，零框架内容。项目里该开
`erasableSyntaxOnly` + `verbatimModuleSyntax`，但那是项目配置，不是框架发现。

## B7 · `init-workflow` 不建 `.gitignore`

**实例**：新 Node 项目 `git add -A` 把 `node_modules` 全 staged，
`freeze-target` 冻结出 **682 个文件**的审查目标。

**状态**：**未修**。`init-workflow` 目前只「建议」`.gitignore` 条目，不创建文件。

## B8 · 冻结的是全仓 diff

审计列为未测场景，项目二立刻撞上（见 B7）。大仓上会更严重。

**状态**：**未修**。

## B9 · ~~数值断言的浮点陷阱~~ → 剔除（判据③：通用知识）

`0.0110-0.0095` 那组夹具算出 `0.14999999999999997`、`===` 不成立 ——
IEEE 754 的基本常识，任何写数值测试的人都该知道，与 agent 工作流无关。

它出现在这份清单里，只是因为它恰好发生在这两个项目上。

## B10 · 「异构 reviewer」从未被真正满足

设计要求跨厂商（如 CC 实现 → codex 审）。实际两次实跑都是**同厂商跨模型**（sonnet 实现 → opus 审）。

> ⚠️ **本条原写「环境限制，未满足」——「环境限制」四个字是错的。**
> 2026-08-08 实测：本机装有 `opencli`，且多家模型**已登录**：
> `chatgpt` / `gemini` / `deepseek` 均为 `Connected, Login: Yes`，
> 并有现成命令 `opencli <厂商> ask "<内容>"`。
> **不是环境不允许，是从来没人试过这条路。**
> 这是同期第 5 次把「做不到」说满（前四次：A15 / A17 / A9 / A13d）。

**为什么「大号审小号」不算数**：同厂家模型的训练数据与思维习惯高度重合 ——
**犯错的倾向**和**看不见错的倾向**是同一个来源。能抓手滑，抓不到盲区。

**实跑佐证**：L2 审查者跑了变异测试、逐字重放上轮变异体、还自造新变异体，抓到了真问题。
但「check 换个 shell 结论翻转」（B12）、「冻结记录的 commit 号其实是 git 报错」（C8）、
「『只读某两节』根本无法执行」（B13）—— **一条都没抓到**，全是编排者后来自己撞见的。
这几条不需要更聪明，**只需要一个不按这套思路想问题的人**。

**状态**：**未满足**。当前采用临时办法：**同模型 + 不同提示词切入角度**
（身份 / 顺序 / 输入 / 姿态 / 切面 五个维度，已写进 `wf-build`）。

⛔ **临时办法不许被记成「异构审查已满足」** —— 换提示词换的是**搜索顺序**，
不是**知识盲区**；同一个模型不知道的事，换什么问法都还是不知道。

**真异构的落地路径见 `ROADMAP.md` 第 1 条**（路已探明，待实跑）。

## B11 · check 的路径基准与命令基准不是同一个，导致从任何目录都跑不通

**现象**：守卫脚本装在项目外面（agentflow 里），`check` 只能用相对路径引它 ——
但那个相对路径的基准是 feature 目录，而被包的测试命令需要的基准是**项目根**。两个基准对不上：

```
从 feature 目录跑：脚本找得到，node --test 找不到用例  → pass 0，FAIL
从项目根跑：      The argument '../../../../agentflow/scripts/check-ac.ps1'
                  to the -File parameter does not exist  → EXIT=-1
```

**后果**：两个项目里**所有** machine 判定的 check，从写下来到被发现，**一次都没成功执行过**。
Builder 跑的是没包守卫的原始命令（能跑），完整 check 直到编排者手动去跑才第一次被点火。
**整条流水线的燃料从未被点火测试过。**

**已修**：`init-workflow` 把 `check-ac.ps1` 随项目下发到 `<项目>/.workflow/bin/`，
`check` 一律写成从项目根出发的 `.workflow/bin/check-ac.ps1`，项目自包含，
也不再依赖 agentflow 装在哪。两个项目已回填并验证。

**讽刺之处**：这个守卫存在的理由就是防「命令没真跑」，而它自己从写下来到被发现，一次都没真跑过。

## B12 · check 里的嵌套转义引号不可移植，同一字符串换个 shell 结论翻转

**现象**（L3 evaluator 报告，编排者独立复现）：

```
写的是   -Cmd "node --test --test-name-pattern \"screener AC-1\"" -MustMatch "screener AC-1"
bash：   matched 5 次  → PASS  exit 0
PS 收到  -Cmd 'node --test --test-name-pattern " screener AC-1\ -MustMatch screener'
PS：     matched 0 次  → FAIL  exit 1
```

PowerShell 把 `\"` 里的反斜杠吃掉后引号错配，**连 `-MustMatch` 参数本身都被吞进了 `-Cmd`**。

**危害等级**：这次表现为**假 FAIL**（实现是对的，执行者拿到失败结论）。
比假 PASS 温和，但同样致命 —— 它会让一个正确的实现被打回重做。
反过来也可能构造出假 PASS，取决于错配后残留什么。

**根因不是转义写错，是 `check` 从没规定过由哪个 shell 执行。**
一条同时要在 bash 和 PowerShell 下成立的命令，就不能依赖任何一方的转义规则。

**已修**：改用 `--flag=value` 形式 + 正则 `\s` 代替空格，把嵌套引号彻底消掉：

```
-Cmd "node --test --test-name-pattern=screener\sAC-1" -MustMatch "screener AC-1"
```

只剩一层引号，两个 shell 解析结果一致（已实测：两边都 `matched 5` / PASS / exit 0）。
`validate-plan` 已加规则：`check` 含 `\"` 直接判 ERR（已做反向测试，确认能拦下）。
两个项目共 12 条 check 全部回填。

## B13 · 「只读文件的某几节」在工具层面不可执行

**现象**：`wf-eval` 的隔离清单写着「允许读 `spec.md` 的『要解决什么』和『范围』两节」。
但 `Read` 返回整个文件，没有按小节取的能力。evaluator 如实披露：
它实际看到了全部 75 行，包括「关键约束」表（开口公式、SMA 种子、分榜条件、门槛数值）。

**后果**：隔离清单里写了一条**无法被遵守**的规则。
evaluator 只能事后申报，而不是事前避免 —— 这让隔离从「机制」退化成「自觉 + 报备」。

**修法方向**（未实施）：要么把可读内容抽成独立文件（如 `spec-public.md`，由编排者生成），
要么改成「允许读整个 spec.md」并承认这个泄漏面，不要写做不到的规则。
**当前写法最糟**：既没防住，又让遵守者背上违规记录。

---

# C 类：作者自己代码的 bug（不是方法论发现）

> 这五条**全部已修**，且**没有一条是发现**。
> 它们逐条列在这里只为一个目的：**让「框架发现了 N 条问题」这个数字不能把它们算进去。**
> 因此只留一行，不展开 —— 展开会让它们看起来像研究成果。

| # | bug | 修法 |
|---|---|---|
| **C1** | PowerShell 5.1 按 ANSI codepage 读无 BOM 的 `.ps1`，中文注释乱码导致引号配对失败、整脚本语法错误 | 全部加 UTF-8 BOM |
| **C2** | `@($null)` 长度是 1 不是 0，缺省 JSON 数组字段变成一个 null 元素，`ContainsKey($null)` 抛异常 | `AsList` 辅助函数过滤 null |
| **C3** | `(Where-Object …).Count` 单匹配时返回 `$null`（PS 5.1 里单个对象不是数组），汇总行显示为空 | `@()` 包裹 |
| **C5** | `validate-plan` 的正则对 markdown 太死板，`- **不做**：`（带加粗）被判成空 | 容忍强调符号与不同列表符号 |
| **C6** | 模板 JSON 里用 `\\"` 塞说明，在 JSON 中是「反斜杠 + 结束引号」，直接写坏模板 | 改用中文引号「」绕开转义 |
| **C4** | `freeze-target` 用 `Out-File -Encoding utf8` 写盘（加 BOM），却对**内存字符串**算 hash，两者不符 → `-Verify` 永远对不上 | `WriteAllText` + UTF-8 无 BOM。**由 L2 reviewer 执行 `wf-review` 规定的核对时抓到** |
| **C8** | `freeze-target` 把 `git rev-parse HEAD` 的**错误输出当成 commit 号**存进 `baseCommit`。非 git 仓库或无提交时，字段值是一整段 `fatal: ambiguous argument 'HEAD'…` | 见下 |

### C8 详情（2026-08-07 新发现）

实物证据，`ema-screener/.workflow/features/screener/review-target.json`：

```json
"baseCommit": "fatal: ambiguous argument 'HEAD': unknown revision or path not in the working tree.…"
```

**后果**：冻结记录里最关键的「以哪个基线为准」这一项是垃圾，
独立方拿到这份 `review-target.json` **无法重建审查基线**。
`sha256` 那一项是好的，所以 `-Verify` 仍然能用 —— 这也是它一直没被发现的原因。

**怎么被发现的**：撤回 A17 时顺手打开这个文件看的，**不是任何检查抓到的**。

## C7 · ~~编排者的临时核对脚本反复出错~~ → 已提升为 **A18**

复盘判定：原条目自己就写着「不是框架 bug，但值得单列」——
既然值得单列，就不该待在「作者代码 bug」里。
它指向的是**判定链最后一环没有守卫**这个流程缺口。正文见 A18。

## C7 · ~~编排者的临时核对脚本反复出错~~ → 已提升为 **A18**

复盘判定：原条目自己就写着「不是框架 bug，但值得单列」——
既然值得单列，就不该待在「作者代码 bug」里。
它指向的是**判定链最后一环没有守卫**这个流程缺口。正文见 A18。

---

# 尚未处理的开放问题

| # | 问题 | 为什么还没修 |
|---|---|---|
| ~~**A9**~~ | ~~验收标准把技术栈锁死在选型之前~~ | ✅ **已修** —— 阶段 0 改成写 `checkIntent`（判据，禁技术词，机械拦截），阶段 1 由 `wf-plan` 具化成 `check`；`gates.json` 在空目录上标 `_provisional`。核实时还修正了本条性质：原以为是硬锁死，实为锚定 + 无人被告知可改；按「业务梳理只讨论业务」在源头拿掉 |
| ~~**A11**~~ | ~~变异测试不在框架里~~ | ✅ **已修** —— `tasks.json` 加 `mutationTargets`；`validate-plan` 对 machine 判定的 AC 强制；`wf-build` 定 Builder 义务；`wf-review` 定 reviewer 的对照组 + 自造变异体。**上线当天在 `pipeline.ts` 上抓到 5/6 存活。**剩余：只有一个项目的数据 |
| **A13a** | 期望值污染任务书 | 🟡 **已加警告** —— `validate-plan` 检测「观察标记词 + 数值」，带推导依据的放行。ema-screener 实测命中 3 条。只能是警告：分不清「实测的期望输出」与「设计参数」 |
| ~~**A13b**~~ | ~~发现泄漏进 skill 正文~~ | ⛔ **撤回** —— 使用者澄清那次改动源于一条口误的提示词，是一次性外部事件，不是流程产生的 |
| **A13c** | 审计的视野边界由作者划定 | 🟡 **可改善** —— 已写入「如何派一次真正的审计」：只给产物、不给主张，问「能证明什么/证不明什么」而非逐条核实。**尚未按新派法实跑过** |
| **A13d** | **判定者与设计者是同一个** | 🟡 **核心不能修，但已做两件** —— ① **框架指纹**：开跑时把规则文件拍照存进 run.json，之后比对；漂移且无书面说明即 exit 3（不禁止改框架，只要求留一句为什么）。② **结论来源统计**：把「编排者自证 N 条」变成每次都打出来的数字。二者都不产生第二个判定者，只是让「题目被改过」查得到、「只有我说行」数得出来。**真解法仍是使用者从 1 变成 2** |
| ~~**A14**~~ | ~~`then` 多子句 vs `check` 单布尔~~ | ✅ **已修** —— `check-ac.ps1` 的 `-MustMatch` 改成多锚点，`;;` 分隔，**全部命中才算通过**，缺哪句直接点名。已三向测试（单锚点向后兼容 / 多锚点全中 / 缺一个精确报出）。⚠️ 踩到 `powershell -File` 不做数组绑定的坑，所以约定 `;;` 而非逗号数组 |
| **A15** | 规则写了但没人执行，且无人检查 | 🟡 **部分已修** —— ① `check-artifacts.ps1` 已寄生到 `validate-plan` 与 `gate-l1`（两条必经之路）；② 新增**轮次对账**：磁盘上有 `l1-round3.json` 而 `rounds` 里没有第 3 条 → 报警；`rounds` 里的 `*_evidence` 指向不存在的文件 → 报警。**剩余**：`inflight` 仍靠编排者自觉写；eval / keep 之后没有任何门 |
| **A16** | 判定的隐含前提无处安放 | ✅ **已修** —— `acceptance.json` 加 `preconditions` 字段，`validate-plan` 对 agent 判定的 AC 强制非空（已反向测试：ema-screener 的 AC-6/7 被拦下并回填） |
| ~~**A17**~~ | ~~冻结机制没有消费者~~ | ⛔ **整条撤回** —— 断言本身是错的，机制完整接通（`wf-build:171` 创建 + `wf-review:18` 强制核对）。见 A17 |
| **A18** | 编排者的核对脚本无人检查 | 🟡 **已缓解，未解决** —— `wf-build` Step 4b 定了三条规则（证伪优先 / 双路复算 / 落盘）；`run.json` 加 `*_verified_by` + `*_evidence`；`check-artifacts` 卡住「标了非 agent 判定就必须指得出证据文件」。**机械只能保证结论指得出证据，保证不了结论是对的 —— 编排者上面没有人（见 A13）**。已在 ema-screener 上跑通全环：标注 → 检测到缺证据 → 补 `orch-check-r3.md` → 干净 |
| **A19** | **框架能处理 agent 死了，不能处理自己死了** | 🟡 **部分已修** —— `check-artifacts.ps1` 解掉「恢复时靠猜」与「孤儿产物无人认领」。剩余：中断恢复没有强制流程；被中断 agent 的部分产物归属规则只写在报告里，未机械化 |
| ~~**B7**~~ | ~~`init-workflow` 不建 `.gitignore`~~ | ✅ **已修** —— 改成**直接创建**（按项目类型给 node/python/go/rust 预设），已存在则只追加缺的条目。⚠️ 忽略清单里**故意不含 `.workflow` 下任何东西**：早期版本建议忽略 `run.json`/`reports/`，那个判断是错的 —— 它们是证据链，忽略掉等于把审计那条「L2/L3 报告一份都不存在」的指控制度化 |
| ~~**B8**~~ | ~~冻结的是全仓 diff~~ | ✅ **已修** —— `freeze-target` 改成按 `tasks.json` 声明的文件范围冻结。实测：44 个 staged 文件 → 冻结范围 2 个，diff 从全仓缩到 298 字节。**顺带收获**：一旦有了声明范围，「改了但不在任何 task 的 files 里」就变成可机械检测的 —— 越界文件单独列进 `review-target.json` 的 `outOfScope`（见 A6：agent 无法自证边界） |
| **B10** | 真异构 reviewer | 🟡 **临时办法已落地** —— 同模型 + 不同提示词切入角度（五个维度，写进 `wf-build`）。⚠️ 「环境限制」是错的说法：`chatgpt`/`gemini`/`deepseek` 经 opencli **均已登录**，命令现成，只是没人试过。真异构的落地路径见 `ROADMAP.md` 第 1 条 |
| ~~**B13**~~ | ~~「只读某几节」在工具层面不可执行~~ | ✅ **已修** —— 按文件切分而非按小节：`spec.md` 整份可读，提示性内容（未决风险 / 怎么确认算对了）移到 `spec-internal.md`（L3 禁读、方案 agent 必读）。**没选「生成删减副本」那条路**：副本每轮都要重新同步，而没人会记得（A15 的三个数据点就是这么来的），过期的副本比泄漏更糟 |
| — | `--concurrency` 之类的 flag 与实际限速器脱节 | 属项目二的业务缺口，但暴露了「冻结签名后无法注入配置」这个模式 |

## 审计指出的，与后续核实结果

| 审计原话 | 后续核实（2026-08-07） |
|---|---|
| README 的 13 条能力主张：**成立 5、部分成立 5、不成立 3** | **未处理**，README 尚未重写 |
| README 的「实跑数据」12 个数字：**只有 5 个可复核** | **更糟了** —— 项目一已删除，其相关数字此后永远不可复核 |
| 熔断规则「连续 2 轮 blocking 指纹相同」不可执行，因为 `run.json` 没有 fingerprint 字段 | ⚠️ **表述需修正**：`templates/run.json` 的 `_roundShape` 里**定义了** `fingerprint`。真实问题是**没有任何环节去填它**（即 A15）。项目二收尾时首次真实填充（round1 = `sha256:9eb0b3ac…`，round2 = 空集） |
| 四条熔断规则**全部零触发** | ⚠️ **这个结论本身不可靠**（判据④）。熔断的触发条件是「连续 N 轮不收敛」，而**没有任何一次运行是自然跑到那么多轮的** —— 会话先被配额切断了。所以「零触发」既可能是「规则没用上」，也可能是「压根没跑到能触发的地方」。**在一个能跑完整流程的环境里重测之前，不该拿这条说事** |
| L3 判失败后的主循环分支**从未走过** | ⚠️ **部分走过了**：项目二 L3 round1 判「不通过」（1 条 blocked），随后进入 round 2 解阻塞。但 round 2 的两个 evaluator 均被编排者主动终止（一次因无痕前提作废，一次因与用户手工操作冲突），最终由**人工**判定。所以「L3 失败 → 自动重派 → agent 重判」**仍未走过 —— 而且这次的阻断者是编排者，不是框架** |
| `wf-keep` 的完整生命周期（observed → validated → retired）**从未走过** | ⚠️ **表述需修正**。项目二的 3 条 lesson 确实全停在 `observed`。但项目一的 2 条写的是 `status: validated` —— 而该项目唯一的后续 feature 从未被建过，**这两条是出生时就被直接写成终点的**。所以不是「从未走过」，是**有人跳过了整个过程直接写了终点**。见 A15 第 3 个数据点 |

## 如何派一次真正的审计（A13c 的改法）

**⛔ 不要这样派**（第一次审计的实际派法，价值有限的原因）：

> 请审计这套框架。以下是它的 13 条能力主张，请逐条核实是否成立。

问题不在于审得不认真，而在于**「该关注什么」已经被我交代完了**。
审计只能在我给的框架内说话，永远发现不了我压根没想到要问的东西。

**应该这样派**：

```
这是一个项目的 .workflow/ 目录和它的代码仓库。

不要问我任何问题。
不要读任何我写的说明性文档（README / KNOWN-ISSUES / AUDIT）。
不要采信任何 agent 报告里的自述结论 —— 只看它贴出的原始命令与输出。

回答一个问题：
  **这些产物能支撑什么结论？不能支撑什么？**

对每一条「不能支撑」，指出缺的是哪一份产物、或者哪一步没有留下痕迹。
```

三处关键差别：

| | 旧派法 | 新派法 |
|---|---|---|
| 谁定义问题 | 我 | **审计方** |
| 输入 | 我的主张 + 产物 | **只有产物** |
| 输出形态 | 逐条「成立/不成立」 | **「能证明什么 / 证不明什么」** |

**为什么第三点重要**：逐条核实只能确认我列出来的那些；
而「证不明什么」会直接暴露**证据链的洞** —— 那正是这份清单里最有价值的几条的来源。

> ⚠️ 即便这样派，**派单的仍然是我，采纳哪几条的也是我**。
> 这条只解决 A13c（审计的视野被我限定），解决不了 A13d（判定者与设计者同一）。

## 从未被检验的场景

| 场景 | 最可能怎么失效 |
|---|---|
| **非 Windows** | 验收命令本身内嵌 `powershell -File`，非 Windows 上流水线**永远无法完成** |
| **非 Node 项目** | gates 预设有 python/go/rust，但一次都没跑过 |
| **存量大仓** | 冻结全仓 diff；文件级 DAG 在耦合代码上大概率切不开（见 A4） |
| **纯库项目** | 没有「真实入口」，L3 会退化成集成测试，防 overfit 效果打折 |
| **多人协作** | `.workflow/` 的并发写入、lesson 的合并冲突，全无设计 |

---

# 一个数字

## 复盘前

**A 16 / B 13 / C 7 = 37 条**，作者此前把三类统一称作「框架层面的发现」。

## 复盘后（2026-08-07 按剔除标准过了一遍）

| 处置 | 条目 | 理由 |
|---|---|---|
| **剔除** 4 条 | B3、B5、B6、B9 | 判据①②③：与本框架无关 / 自己制造 / 通用知识 |
| **合并** 1 条 | B2 → B1 | 同一个机制（空过）的两个入口，分开列是虚增 |
| **提升** 2 条 | C4 → A17，C7 → A18 | 不是脚本 bug，是设计级失效与流程缺口 |
| **改写** 1 条 | A15 | 判据④：三个分论断里**两个被证伪**（skill 正文里明文要求了），只剩窄结论 |
| **降级** 1 条 | 「四条熔断规则零触发」 | 判据④：从没有一次运行自然跑到能触发的轮数，会话先被配额切了 —— **这个结论本身不可靠** |

**实存 33 条：A 18 / B 8 / C 7。**（A17 整条撤回，不计入；C 类新增 C8）
其中可称为「框架发现」的是 **A 18 + B 8 = 26 条**；
C 类 7 条是我自己的 PowerShell / JSON / git 低级错误，**不计入**。

## 这次复盘本身说明的事

37 → 27（复盘中新增 A19），剔除与改写掉的**接近三成**。而这份清单是我一边开发一边记的，
每一条当时都真诚地觉得是个发现。

虚增的来源有三类，都不是故意的：

1. **把「这台机器上这个工具版本的行为」当成框架发现**（B5、B6、B9）——
   它们确实发生了，也确实卡住了流程，但换掉 agentflow 照样存在。
2. **把「自己制造的误报」当成发现**（B3）——
   我先加了一条过严的规则，它误报，我再加个豁免，然后把这一轮记成一条发现。**循环论证。**
3. **把「没跑到」当成「没有」**（A15、熔断零触发）——
   环境是 5 小时配额窗口且频繁中断，
   而我把「跑到一半断了所以没产出」直接写成了「框架没要求产出」。
   **A15 是最典型的一例：三个分论断里两个，去 skill 正文里 grep 一下就能证伪。**

第 3 类最危险，因为它**看起来像最严厉的自我批评** ——
「连自己的规则都没落实」听上去比「工具版本有个 bug」深刻得多，
所以更不容易被质疑。实际上它只是没做核实。

这正好是用户最早问的那个问题的实物形态：**边写边修 bug，会过拟合。**
过拟合不只体现在框架的规则上，**也体现在「框架发现了什么」这个记录上**。

## 下次的纪律

1. 一条发现要进清单，先过四条判据。
2. **写「从未 / 零 / 没有」之前，先 grep 一遍 skill 正文**（判据④）。
   搜到要求 = 执行缺陷，搜不到 = 设计缺陷，两者修法相反。
3. 拿不准的先记进 feature 目录，**等第二个独立项目撞到同一件事再提上来**。
4. 涉及「某机制没生效」的，优先找**可 grep 的证据**（如 A17 那样），
   而不是「我没看到它发生」。

## 这份清单剩下的最大弱点

**它仍然只有一个数据点。**项目一已删除，项目二是唯一的实跑。
A 类 18 条里，能拿出两个独立数据点的只有 A15 一条（项目二的 run.json 缺失 +
项目一 README 记录的「唯一违反者是编排者」）。

**其余 17 条都是单点观察。**按上面第 3 条纪律，它们严格说都还不够格进这份清单 ——
留着是因为删了就什么都不剩了，但**读的时候要知道这一点**。

---

# 两条模式

## 模式一：`check` 从没被当成「需要自己被测试的产物」

B11 和 B12 是同一件事的两面。

`check` 是一条要在未知目录、由未知 shell、在未来某个时刻执行的命令，
却从写下的那一刻起就没人跑过它 —— Builder 跑的是它的内层，
L1 跑的是别的命令，编排者跑的是自己临时拼的版本。
**它唯一一次被原样执行，是在 L3 evaluator 手里，而那已经是流程的最后一环。**

`validate-plan` 现在能静态拦掉两种写法错误（缺守卫、嵌套引号），
但**静态检查拦不住「这条命令在这台机器上跑不通」**。
真正的修法是让 `check` 在被写下的那一轮就冒烟执行一次
（`init-workflow` 已经对 `gates.json` 这样做了，`acceptance.json` 还没有）。**未实施。**

## 模式二：框架要求别人拿证据，却不要求自己留证据

A15 和 A16 指向同一处矛盾。

这套东西的每一层都在喊「没有证据的通过不算通过」：
L1 要退出码，L2 要冻结 hash，L3 要 screenshot / log / http-trace，
`wf-eval` 甚至规定「判 pass 却拿不出证据的，改判 blocked」。

**但框架自己的运行事实没有任何一条是强制落盘的。**
`run.json` 可以不写，L2 正文可以不写，派发决策**明文规定**不写。
于是「四条熔断规则零触发」「决策矩阵被走过」这类关于框架自身的论断，
和它所禁止的那种「无证据的通过」是同一个东西 —— 只是换了个主语。

A16 更进一步：连**判定成立的前提**都没有字段可以安放，
只能靠派发方在 prompt 里口头补。补对了是运气，补漏了就产生一份
**形式无懈可击、结论完全错误**的验收报告 —— 而隔离机制拦不住这种错误，
因为它防的是「看了答案」，不是「前提缺失」。

**修这两条比修任何单个 bug 都便宜**：加几个必填字段、加几条 validate 规则而已。
拖着不修的代价是，这份清单里越来越多的条目会变成不可复核的自述。
